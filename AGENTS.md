## Agent skills

### Issue tracker

任务和规格以本地 Markdown 保存于 `.scratch/`。详见 `docs/agents/issue-tracker.md`。

### Domain docs

采用 single-context：根目录 `CONTEXT.md` 和 `docs/adr/`。详见 `docs/agents/domain.md`。
