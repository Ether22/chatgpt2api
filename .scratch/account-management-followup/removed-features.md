# 上一轮删改清单：供用户逐项选择

核对时间：2026-09-08。比较改造前 `dc105e5` 与交付后 `24abdb6`，覆盖 `api/`、`services/`、`web/`、`utils/` 全部差异，并交叉核对十八票规格和报告。核查为静态代码对比，未读取真实账号/配置、未运行消费请求、未删除数据。文中“新图”指网页或异步图片任务产生并由服务器会话管理的图片；同步 `/v1/images/*` 产生的非托管图及旧非托管图不一定受同样限制。

此文记录的是产品行为删改，不把内部函数删除当功能删除，也不把本来没有的功能算成被删掉。下表保留选择前的建议和基线行为；用户于2026-09-09已逐项选择，最终决定以 [spec.md](spec.md#2026-09-09追加确认) 为准，不将旧建议误当成最新要求。

用户追加要求：分别列明“原来是什么”“当前实际是什么”“建议怎么处理”。**当前**均指尚未实施本轮恢复的24abdb6应用代码；**建议/已确认方案**不表示已经生效。

## 已明确恢复或修改，无需重复选择

- 恢复“删除所选”“移除异常账号”及真实手动删除实现；原 `DELETE /api/accounts` 从可删除改成405，也要随这两个按钮恢复。
- 恢复“自动移除异常账号”“自动移除限流账号”两个设置开关。异常凭据确认失效才自动删，暂时网络/验证码/地域问题保留；仅监控、禁用账号自动删除豁免。
- 去掉账号隐藏，包括显示隐藏筛选、逐行隐藏/找回及列表过滤；原隐藏账号重新显示，不删除账号。
- 导出全部账号默认可回导JSON，保留登录来源与刷新凭据；TXT单独保留。**原顶栏TXT本来就只包含Access Token，这不是上一轮新删的能力，而是此次一并修复的旧缺陷。**
- 时间显示去掉“北京时间”后缀，仍使用原时区；账号整行拖动、让位、落位及保存失败回位动画。

| 已确认项目 | 当前实际行为 | 已确认的修改方案，尚未实施 |
|---|---|---|
| 删除所选、移除异常账号 | 两个按钮都没有；旧账号DELETE接口返回405，不能删除。 | 恢复按钮和实际删除链路。 |
| 两项自动移除 | 设置开关没有；认证异常或限流只更新健康状态，账号保留。 | 恢复两个独立开关；确认凭据失效才自动删异常，限流另控，监控/禁用免于自动删。 |
| 账号隐藏 | 可隐藏/取消隐藏；默认列表过滤隐藏项，“显示隐藏账号”可找回。 | 去掉整个账号隐藏功能，原隐藏账号重新显示。 |
| 导出全部账号 | 顶栏输出纯Access Token TXT；现存JSON导出要求三种Token齐全且固定Codex类型，导入会覆盖来源。 | 默认导出所有账号的凭据与来源JSON，正确回导，TXT另列选项。 |
| 时间文字 | 日期/时间后附“北京时间”。 | 只去掉这四字，保留时区。 |
| 拖动排序 | 仅拖拽柄，无整行跟随/邻行让位；保存成功后列表直接换位。 | 整行跟随、实时让位、平滑落位、保存失败回位。 |

## A：撤掉、替代或收窄的具体能力

“恢复”均指用当前状态与持久化规则正确实现该能力，不照抄旧代码的错误路径。编号可直接用于回复。

| 编号 | 原来是什么 | 当前实际是什么 | 建议怎么处理 | 旧版本及当前证据 |
|---|---|---|---|---|
| A01 | 每行有单账号垃圾桶按钮。 | 没有单行删除；行操作仅隐藏、编辑、刷新。批量删除目前也未恢复。 | 恢复单行删除。 | 旧 `web/src/app/accounts/page.tsx:1190`；[当前行操作](../../web/src/app/accounts/page.tsx:1153) |
| A02 | 刷新账号、密码恢复过程中，顶部统计实时变化。 | 只更新处理进度，卡片等全部结束才更新。 | 恢复准确的实时统计，不复用旧的不准确推算。 | 旧 `accounts/page.tsx:359,547`；[刷新进度](../../web/src/app/accounts/page.tsx:413)、[恢复进度](../../web/src/app/accounts/page.tsx:560)；[票15承认的取舍](../image-workflow-upgrade-delivery/reports/15.md:15) |
| A03 | 编辑账号可手动设健康“正常/限流/异常/禁用”。 | 界面只可选用途“正常使用/仅监控/禁用”；旧健康状态更新API仍在。 | 恢复独立健康状态编辑，与用途分开；上游刷新仍可更新健康。 | 旧 `accounts/page.tsx:785`；[当前编辑](../../web/src/app/accounts/page.tsx:742)、[保存](../../web/src/app/accounts/page.tsx:642) |
| A04 | “禁用账户”卡显示上游停用数量。 | 卡片统计用户选择的禁用用途；上游停用只保留列表标签/筛选，没有独立计数卡。 | 补回上游停用计数，保留用户禁用统计。 | 旧 `accounts/page.tsx:274`；[当前统计](../../web/src/app/accounts/page.tsx:307)、[健康筛选](../../web/src/app/accounts/page.tsx:75) |
| A05 | 数量选择有1–10张十个快捷按钮及原生数字步进箭头。 | 只剩文本输入框，没有快捷按钮和步进箭头。 | 保持当前输入框。 | 旧 `image-composer.tsx:81,492`；[当前数量输入](../../web/src/app/image/components/image-composer.tsx:503) |
| A06 | 读取浏览器IndexedDB中的生图历史。 | 只读取服务器历史，未迁移旧浏览器记录；旧记录未被这次代码清空，但新页面不读。 | 维持上一轮已选的重新部署、不迁移；需要旧历史时再加入迁移。 | 旧 `store/image-conversations.ts:65,228`；[当前读取](../../web/src/store/image-conversations.ts:181)；[原范围决定](../image-workflow-upgrade/spec.md:266) |
| A07 | 读取 `image_tasks.json`、`image_index.json`。 | 只读取新SQLite，旧JSON留在原处但不导入；仅旧索引登记的远端图不恢复显示。 | 维持不迁移；如需旧历史则实施兼容迁移。 | 旧 `image_task_service.py:359,407`、`image_storage_service.py:178`；[新存储入口](../../services/storage/image_rows.py:12) |
| A08 | 手工复制图片进images目录，刷新图库即可自动发现。 | 普通刷新只查索引，不扫描目录，因此不会自动发现新放入的文件。 | 增加显式“扫描本地图片”入口，日常继续分页读取。 | 旧 `image_storage_service.py:271,276`；[图库读取](../../services/image_service.py:148)、[分页索引](../../services/image_storage_service.py:397) |
| A09 | 图库灯箱可在全部筛选结果中连续左右浏览，跨12张页边界。 | 灯箱只有当前页12张；看下一页需关闭灯箱、翻页、重开。 | 恢复跨页连续浏览，按需加载。 | 旧 `image-manager/page.tsx:100,109`；[当前灯箱数据](../../web/src/app/image-manager/page.tsx:113)、[灯箱入口](../../web/src/app/image-manager/page.tsx:715) |
| A10 | 灯箱显示文件KB/MB及像素尺寸。 | 只显示尺寸/序号，不显示KB/MB；新生图结果卡片通常也没有体积。 | 恢复体积显示。 | 旧 `image-lightbox.tsx:357`；[当前信息区](../../web/src/components/image-lightbox.tsx:460)、[卡片](../../web/src/app/image/components/image-results.tsx:245) |
| A11 | 图库中可单删、批量删、按日期删图片。 | 指定新托管图返回409；按日期/全部删除跳过新图。新图要到生图会话删除，旧非托管图仍可在图库删。 | 恢复图库删除新图，接入现有引用与后台清理规则。 | 旧 `api/system.py:111`；[当前拒绝](../../api/system.py:127)、[范围过滤](../../services/image_service.py:173) |
| A12 | 保留天数清理历史图片及终态任务。 | 新托管结果、素材及任务不自动过期，要显式删除；旧非托管图仍按天数清理。 | 维持新历史不自动过期。 | 旧 `config.py:533`、`image_task_service.py:423`；[当前图片清理](../../services/config.py:516)、[任务清理](../../services/image_task_service.py:1569) |
| A13 | “释放到目标空间”可删除最旧PNG。 | 功能仍在，但跳过新托管图片，不能靠它清理新生图历史。 | 保持不自动删新历史；用明确的历史删除。 | 旧 `image_service.py:278`；[当前空间清理](../../services/image_service.py:282) |
| A14 | “压缩全部图片”处理全部PNG。 | 功能仍在，但跳过新托管图片。 | 保持新图原文件不变。 | 旧 `image_service.py:254`；[当前压缩](../../services/image_service.py:259) |
| A15 | 已发图片请求遇超时、只回文字等失败，可自动换号重新生成。 | 已发送后不再自动重新生成；可恢复时核实原请求。发送前明确未消费的重试仍在，网页异步与共用同步协议均受影响。 | 保持不自动重复消费。 | 旧 `protocol/conversation.py:1395,1440`；[现超时](../../services/protocol/conversation.py:1401)、[只回文字](../../services/protocol/conversation.py:1448)、[异常](../../services/protocol/conversation.py:1501) |
| A16 | “始终删除上游会话”对成功、失败都执行。 | 开关仍在；异步任务只在成功或恢复成功时清理，确定失败也跳过。同步路径仍用旧finally。 | 恢复确定失败后的清理；结果未知时等核实后处理。 | 旧 `conversation.py:813,1369`；[当前移交](../../services/protocol/conversation.py:1385)、[异步清理](../../services/image_task_service.py:1389) |
| A17 | “删除Prompt”清空该轮正文，不能再复用该正文；不代表擦除所有服务端/上游记录。 | 仅隐藏展示，原正文和生成配置保留、还能复用。 | 保持隐藏和快照；真正删除历史用轮次/会话删除。区别于本轮去掉的账号隐藏。 | 旧 `image/page.tsx:973`；[当前隐藏](../../web/src/app/image/page.tsx:956)、[隐藏后展示](../../web/src/app/image/components/image-results.tsx:184) |
| A18 | 单张重生在原位置替换失败图片。 | 新建一张图的新轮次，原失败图片继续保留。 | 保持新轮次与历史可追溯。 | 旧 `image/page.tsx:1394`；[当前单图重跑](../../web/src/app/image/page.tsx:1210) |
| A19 | 忽略失败会减少该轮数量并重新连续编号。 | 隐藏失败项，不改原生成数量及序号，可能有缺号。 | 保持原轮次数量/编号。 | 旧 `image/page.tsx:1506,1521`；[当前忽略](../../web/src/app/image/page.tsx:1247)、[服务端](../../services/image_task_service.py:290) |
| A20 | 下载失败会自动新开原图。 | 只报错，不自动开原图；还校验PNG/JPEG/GIF/WebP文件头，不识别则拒绝下载。 | 保留鉴权和格式检查，增加明确“重试下载/打开原图”操作。 | 旧 `image-results.tsx:70`；[当前下载](../../web/src/store/image-conversations.ts:287)、[卡片处理](../../web/src/app/image/components/image-results.tsx:299) |
| A21 | “全选结果”复选框可全选，也可一键全部取消。 | 复核更正：24abdb6 已有“取消选择”按钮，既能取消收集，也能清空全部选择；这项能力没有被删。 | 将入口改名为更明确的“取消全部选择”，保留并验证原功能。 | 旧 `image-manager/page.tsx:451`；24abdb6 `image-manager/page.tsx:509`；[当前入口](../../web/src/app/image-manager/page.tsx) |
| A22 | 更改图库日期保留标签筛选和仍匹配的已选图。 | 日期一改，标签筛选和全部已选图都清空。 | 恢复标签筛选和仍匹配的选择。 | 旧 `image-manager/page.tsx:124,283`；[当前日期变化](../../web/src/app/image-manager/page.tsx:331) |
| A23 | 反复把同一历史参考图“继续编辑”会重复追加。 | 同一引用ID只保留一项；重新上传同文件仍可形成另一项。 | 保持按引用去重。 | 旧 `image/page.tsx:1109`；[当前加入参考](../../web/src/app/image/page.tsx:1130) |

## B：没有整项删除，但使用范围或默认行为改变

这些项同样可以按编号提出修改，不能误说成旧能力全部消失。建议均为维持当前行为；B01若需要对外分享，建议增加受控分享链接，B02若需要统一管理，可增加明确管理员视图。

| 编号 | 原来是什么 | 当前实际是什么 | 建议怎么处理 | 证据 |
|---|---|---|---|---|
| B01 | 原图/缩略图可匿名直链，也可用公共图片域名。 | 新托管图需要所属身份授权，不用public_base_url；复制链接仍在，但不能匿名打开。旧非托管及同步生成图保留原行为。 | 保持鉴权，需要分享时增加分享链接。 | [图片路由](../../api/system.py:105)、[URL生成](../../services/image_storage_service.py:276) |
| B02 | 管理员图库可管理全部图片。 | 管理员也不能跨身份看、下载、标记别人的新托管图；旧非托管图仍共享。 | 保持身份隔离，需要统一管理时增加管理员视图。 | [图片归属](../../services/image_storage_service.py:227)、[标签范围](../../services/image_tags_service.py:74) |
| B03 | 图库API一次全量返回，前端12张一页；历史会话和轮次整份加载。 | 图库API默认12、最多100条；会话首批30条再加载，结果默认每页2轮，需翻页/导航。离屏图释放后按需再加载。 | 保持分页；另按A09恢复灯箱跨页。 | [图库API](../../api/system.py:98)、[会话列表](../../web/src/app/image/components/image-sidebar.tsx:180)、[结果分页](../../web/src/app/image/page.tsx:1409) |
| B04 | 无记忆默认1张；小数取整、越界夹取、空值回落1。 | 无记忆默认4张；只接受1–100纯数字整数，非法禁止提交。合法数量记忆仍保留。 | 保持当前默认与明确校验。 | [默认数量](../../web/src/app/image/page.tsx:199)、[校验](../../web/src/app/image/page.tsx:97) |
| B05 | 选图先本地预览，生成时发送；上传文件分支没有50MB校验，URL/Base64已有。 | 选中立即上传，未完成/失败不能生成；普通引用须有效PNG/JPEG/WebP/GIF、非空、≤50MB。多选/拖入/粘贴仍在，无固定张数上限。 | 保持当前上传与校验。 | [参考上传](../../services/image_task_service.py:653)、[multipart读取](../../api/image_inputs.py:317)、[前端提交](../../web/src/app/image/page.tsx:1270) |
| B06 | 没有同样的提交前存储预检，可能生成后保存失败。 | 新异步任务/参考上传遇本地不足500MB、索引不可写或配置的WebDAV不可用时拒绝；WebDAV-only也查本地，同步接口无同样预检。 | 保持提前检查。 | [可写预检](../../services/image_storage_service.py:240)、[生成入口](../../services/image_task_service.py:1235) |
| B07 | 错误文字含超时且有任务ID就显示“继续等待”；旧恢复实现有参数错误。 | 后端明确允许恢复才显示，使用原账号/原请求核实；旧参数错误已修。 | 保持明确的恢复条件。 | [按钮](../../web/src/app/image/components/image-results.tsx:313)、[恢复](../../services/image_task_service.py:1585) |
| B08 | 没有独立监控用途/额度；禁用健康状态跳过定期Token保活，列表按原顺序。 | 可用额度只计可消费用途，监控额度单列、监控组置顶；监控/禁用不消费、不贡献模型可用性，但继续维护。 | 保持当前用途与额度规则。 | [账号统计](../../services/account_service.py:1775)、[保活](../../services/account_service.py:358)、[模型目录](../../services/model_service.py:59) |
| B09 | Prompt全文展开、图片瀑布流；灯箱操作在两侧/顶部；整轮按原数量重生。 | Prompt默认两行可展开、图片逐行网格、操作在底部；整轮重生可填新数量。缩放/拖动/触摸/方向键仍在。 | 保持当前布局和入口。 | [结果布局](../../web/src/app/image/components/image-results.tsx:239)、[重生入口](../../web/src/app/image/components/image-results.tsx:447)、[灯箱操作](../../web/src/components/image-lightbox.tsx:437) |
| B10 | 首次提交才进会话历史；各浏览器独立记忆当前会话；删除历史主要移除本地记录。 | 空会话立即保存、当前会话选择按身份共享；删除历史真实清理服务器文件，影响同身份其他浏览器，保留当前素材和仍被引用文件。 | 保持服务器保存、共享与真实删除。 | [新建](../../web/src/app/image/page.tsx:880)、[选择](../../web/src/app/image/page.tsx:914)、[会话删除](../../services/image_task_service.py) |
| B11 | 图片通常命名image-序号.png；业务时间混用本地/UTC及旧文件名格式。 | 图片按Prompt摘要或MD条目命名，扩展名用真实格式；业务日期/账号备份名含+08:00/+0800。协议UTC、JWT、签名、耗时未改。 | 保持当前名称和时间语义，只按已确认要求去掉界面“北京时间”后缀。 | [结果文件名](../../web/src/app/image/components/image-results.tsx:46)、[账号导出日期](../../api/accounts.py:135)、[备份名称](../../services/backup_service.py:527) |
| B12 | 日志没有这套共用凭据脱敏，可能记录完整Token/授权URL。 | 日志仍在，但凭据和授权URL等会脱敏，其他诊断字段保留。 | 保持脱敏。 | [日志写入](../../services/log_service.py:64)、[脱敏](../../utils/redact.py:10) |

## 确认仍保留，未撤掉

- 账号Access Token、Session JSON、Codex认证JSON、OAuth、账号JSON导入；完整账号导出API；账号刷新、尝试恢复异常、刷新后自动密码登录、账号代理和代理测试、账号复制。
- CPA/Sub2API管理、用户密钥增删改、备份包含选项/R2/加密/下载/删除备份、日志查询和删除，其余设置开关。备份增加SQLite快照，并非删除备份。
- 普通文生图/图生图、模型/质量/比例/自定义尺寸、参考图多选/拖入/粘贴、会话重命名/删除、清空历史、复用配置、整轮/单图重生、忽略失败、符合条件的继续等待。
- 原图下载、图库日期/标签筛选、标签管理、批量ZIP下载、缩放/拖动/触摸/键盘操作；图库全选与取消选择均已存在，A21仅明确入口文案。
- `/v1`文字/图片、搜索、可编辑文件接口及同步Base64响应。JSON远程图片URL此前即支持，不是本轮撤回的功能；新 `file_id` 是增强。
- 基线没有可见的通用暂停/停止生成按钮，不能说本轮删掉该按钮。MD导入是新能力，它的新增限制不属于旧功能删除。

## 本轮建议汇总

建议恢复/补回：**A01、A02、A03、A04、A08（显式扫描）、A09、A10、A11、A16（仅确定失败）、A20（显式重试/打开）、A21、A22**。

以上为选择前的建议，不是最终决定。用户后续覆盖A03、A05、A06/A07、A08、A12–A17、A21/A22、B03/B10，并确认沿用旧自动移除开关值和手动删除不豁免用途。详见 [最终选择](spec.md#2026-09-09追加确认)；当前代码的后续实施状态见该规格进度。
