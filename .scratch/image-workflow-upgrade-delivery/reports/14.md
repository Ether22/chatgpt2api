# 14 验收报告

实际任务01a07dda-14e0-7f12-b85f-0f934f6faca3，独立工作树f52d、分支codex/image-task-14，gpt-6-astra/high；派发基线adcee131387a4653182b93ba0ce7272922687fbe。源任务提交1e1f069后因账户用量限制中断，主任务接手未提交的同网格行高亮修复、浏览器脚本、最终审查与集成验收。

## 实现与逐项验收

- 右侧导航使用来源条目→第N次→原图片编号三层。MD显示文档标识及名称，普通条目使用现有本地Prompt短名；重跑和复用继续使用09保存的原来源，不增设重跑层。
- 来源名称定位该来源最新一轮，独立箭头只展开/收起；收起后仍展示当前位置。点击图片后，同一网格行的其他图片不会抢走用户选择；手动滚动更新来源、次数及图片高亮。
- 复用13的轻量导航接口，每页最多10轮，仅传ID、状态及来源信息，不携带Prompt、参考图、URL或Base64。图片详情仍每页最多2轮，未加载目标通过既有turn_id/image_id定位接口读取，按身份校验。
- 服务端在过滤隐藏轮次之前计算来源次数，隐藏第1次后仍保留第2次的原编号；删除图片、隐藏轮次和删除会话经轮询同步移除导航项。
- 桌面左右栏及中间结果区分别滚动；窄屏使用同一导航的抽屉。320×360下结果区至少240px高，外层可滚动到编辑框、完整数量输入和生成按钮，抽屉末项与关闭可达。
- 最终集成修复了迟到/失败定位高亮、浏览器自动锚定干扰位置恢复、图片首次解码前占位几何、可见性回调批量记录以及跨页定位后的布局校正；具体证据见下。

## 集成与回归证据

源1e1f069集成为c7a3a05；主3ed065b保留源未提交的同网格行选择与三份浏览器脚本。ae4b4da修复旧定位响应晚到覆盖新高亮：A请求暂扣、B先完成、A后返回和A返回404均通过原生浏览器。

60afaeb为缓存尺寸增加原生img宽高属性；037864f关闭结果区原生overflow anchoring，避免浏览器在应用恢复scrollTop后额外调整；5a3c7b9在未知尺寸首次解码之前保留原占位高度。这些修复未扩大图片加载范围或改变原始像素。

8d58b89修复LazyImage只处理第一条IntersectionObserver记录的问题。原生Chrome慢CPU诊断抓到同一图片false→true的批量回调，而旧组件仍保留占位。此API允许同一目标的多条记录在一批交付，见[MDN说明](https://developer.mozilla.org/en-US/docs/Web/API/Intersection_Observer_API#intersection_change_callbacks)。每个LazyImage只观察一个目标，现采用最后一条记录；导航观察器原本逐条处理，无需修改。

新增14-lazy-image-check.cjs复用13的真实PNG滚动/返回流程，只暂存并合并真实观察器记录。旧5a3构建在可见图片解码等待中失败；8d58构建成功，实际6个false→true批次，原位置1200/图片5返回后仍为1200/图片5。另用4倍CPU节流复跑原生观察器场景通过。日志为隔离树data/lazy-regression-red.log、lazy-regression-green.log、visible-slow-normal.log及visible-slow-normal-green.log。

f50f233在既有定位效果内观察当前页内容尺寸变化，使目标解码后继续贴齐结果区；用户手动滚动偏离已应用位置时停止校正。最终审查指出会话对象轮询更新可能提前清理观察器；83a872a将生命周期绑定到会话、页偏移与本次成功定位请求，目标移除也停止观察。该项已独立复审关闭；两种密度最终浏览器均通过真实HTTP改标题→轮询刷新→首轮内容增加/去除40px布局高度后目标仍贴齐、手动滚动后改宽度不被拉回、迟到/404、低高度抽屉和删除。

## 完整自动化与分页

所有检查只在主隔离工作树`.venv/integration-check`运行，受控身份、临时实际文件和本地HTTP；不访问主目录实际data/config，不消费上游额度。

- 组合完整离线：`python .scratch/image-workflow-upgrade-delivery/reports/17-offline-check.py`，340 passed、7 subtests passed，131.39秒。包含14来源次数/隐藏过滤及18C三服务器时区跨午夜测试；之后应用变更仅为前端布局与定位。
- 单独TypeScript检查及生产构建均通过，11个静态页面；构建不代替独立类型检查。
- 最终8d58生产构建运行14-pagination-check.cjs，两种3200实际PNG数据均通过全部13验收：实际可见原图解码、分页、Blob释放/重读、旧响应丢弃、页偏移及像素位置恢复、指定目标权限、图库12项分页、13个选中原图ZIP、标签刷新、全选失败恢复、删除中间续页后仍可访问65个会话。详情最多2轮，轻量导航最多10轮，页面错误0。

| 分页诊断 | 80轮×20张/会话 | 16轮×100张/会话 |
| --- | --- | --- |
| 首开/换会话/上一页 | 789/581/295ms | 2238/1673/480ms |
| 滚动帧间隔P95/最大 | 19.5/27.3ms | 18.9/32.5ms |
| 滚动后输入 | 23ms | 66ms |
| 返回位置 | 1200/图5→1200/图5 | 1200/图5→1200/图5 |
| Blob峰值/此阶段驻留/已释放 | 6/6/69 | 6/6/79 |
| 图库首开/下一页 | 1078/80ms | 1515/128ms |
| 选中13图ZIP | 3178字节 | 3192字节 |

每种数据均为两会话共3200图。上述为本机一次诊断，包含并行验收的影响，不是上游生成耗时或统计性能保证。普通场景无长任务，高密度记录71/116ms两次长任务，未删去这些观测。证据为隔离树data/14-pagination-evidence/[dense/]browser.json及data/final-14-pagination[-dense]-browser.log。

## Standards

主固定f6f2d60..3ed065b审查0行动项；ae4b4da、60afaeb、5a3c7b9、8d58b89、f50f233、83a872a各增量复审均0。复用既有导航接口、版本守卫、原生观察器与图片身份读取，没有新增依赖或通用滚动框架。

## Spec

初次主审发现迟到/失败定位覆盖当前高亮P2，ae4b4da修复并复审关闭。几何及批量可见性修复增量0行动项。f50f233后发现轮询刷新提前结束定位校正P2，83a872a修复并经独立复审确认代码层面闭合；其后两种密度完整浏览器实际通过。最终0剩余行动项。

## 最终导航验收（83a872a）

最终单独tsc与11页生产构建通过。14-browser-check.cjs在43035普通密度、43033高密度两份混合MD/普通来源的3200实际PNG数据上退出0，页面错误0；界面位置、图片解码、高亮、独立滚动、第二浏览器恢复、抽屉及删除断言全部保留。

| 导航诊断 | 80轮×20张/会话 | 16轮×100张/会话 |
| --- | --- | --- |
| 首开（含登录）/跨页定位 | 2126/229ms | 3153/587ms |
| 滚动帧间隔P95/最大 | 18.6/24.8ms | 22.4/37.2ms |
| 滚动后输入 | 26ms | 59ms |
| Blob峰值/删除会话后驻留/已释放 | 6/0/87 | 6/0/64 |
| 导航累计请求/字节 | 24/526148 | 6/465180 |
| 详情请求 | 8 | 8 |

最终轮询刷新、迟到定位、404、手动高亮、手动滚动后释放定位、导航独立滚动、低高度操作与删除标志均为true。上述导航累计数据包含主动刷新与全部交互，不是单次首开响应。证据：隔离树data/14-evidence/{normal,dense}/metrics.json、desktop/results/composer/drawer截图及data/final-14-browser-{normal,dense}.log。最终320×360结果区、编辑框和抽屉截图已目视核验。

Standards 0，Spec 0；第14票已集成并验证完成。全部测试使用受控存储和上游边界，真实上游与远端存储联调不在本次授权测试范围。
