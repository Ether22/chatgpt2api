# 17：账号只保留不物理删除

已完成 US110、US111。旧账号 DELETE 明确返回 405，服务层删除抛出 `ValueError`；手动入口、自动移除分支和旧设置入口已取消。异常与限流只更新健康，账号仍可查询、编辑和维护。

## 工作树与提交

- 任务：`01a07cff-4ece-7c00-9b83-96a96150951d`
- 工作树：`C:/Users/ForestHill/.codex/worktrees/f6ae/chatgpt2api`
- 分支：`codex/image-task-17`
- 派发/审查固定点：`30785289d9494c7d0b04ebfecda411fc138a9ea7`
- 实现：`1c35397283f41d91d2e67ef88e025d977fbca38f`
- 审查修复：`8e591aabfae2d53baa3cb4b7c694fbd443a371c9`
- 使用 implement、tdd、code-review、ponytail；按既有授权执行本地修改、测试与提交，未自行集成或更改唯一 PROGRESS。

## 修改区域与调用链

- `services/account_service.py`：封住 `delete_accounts`；`remove_invalid_token` 保留兼容签名，仅标异常；去掉 `update_account`、`mark_image_result` 两处直接自动 `pop`；新增共用 `mark_rate_limited`，复用 `update_account` 保留字段。上游提供 `Retry-After` 时设置带时区恢复时间，未提供时不覆盖原 `restore_at`。
- `api/accounts.py`：旧 DELETE 保留管理员鉴权、明确拒绝，移除专用请求类。用户密钥的 DELETE 不变。
- `services/config.py`、`web/src/lib/api.ts`、`web/src/app/settings/store.ts`、`settings/components/config-card.tsx`：删除两个旧开关的读取属性、公开输出、类型、序列化、setter 与界面；旧文件/请求中的键即使为 true 也没有自动删除消费者。旧文件里的未知键不做破坏性迁移。
- `web/src/app/accounts/page.tsx`：移除单个删除、删除所选、移除异常账号及专用状态/import；保留编辑、刷新、异常恢复。
- 主对话批准补充 `OpenAIBackendAPI.search` 和搜索 `_wait_search_result`：明确鉴权错误标异常，429 标限流；轮询内部捕获的 429 仍继续原重试与结果返回。未知网络错误保留原健康。
- 主对话批准补充 `services/protocol/conversation.py` 文本/图片异常分支及共用失效分类：识别结构化 401、429 和 `InvalidAccessTokenError`，保留票15资格复核、已有重试与槽位释放。流已输出后也记录明确失效健康。
- 核查 `save_accounts`、账号 `pop` 和删除调用方：剩余 `_accounts.pop(old_token)` 只用于 token 轮换换键，原账号信息被带到新键；未改 StorageBackend 全对象保存/删除语义。未触碰票16隐藏、排序新接口及其测试，未更改票03图片/任务结构。

## 逐项验收

- [x] 单个、批量、移除异常的删除按钮消失；编辑弹窗无删除动作。
- [x] 旧 HTTP DELETE 返回 405，旧服务调用拒绝；不是假成功。真实临时 JSON 存储重载后记录完整。
- [x] 两旧设置不再展示/公开输出，旧配置开启也不能触发删除。
- [x] 三种用途的刷新、登录失败、限流更新、图片完成/失败继续保留账号。
- [x] 文本、图片、搜索明确失效/限流更新健康；保留用途、隐藏、排序、凭据、创建时间和自定义元数据。
- [x] 搜索真实 GET 轮询先 429 后成功仍返回原结果，记录真实 Retry-After；未知网络错误不改变健康。
- [x] 用户密钥仍可独立删除；票15消费资格与维护行为回归通过。

## 测试命令与结果

全部后端命令从本工作树运行，使用 `C:/Users/ForestHill/Documents/chatgpt2api/.venv/runtime/Scripts/python.exe`，环境 `CHATGPT2API_AUTH_KEY=chatgpt2api`、`PYTHONIOENCODING=utf-8`。所有账号均为假值，持久化使用临时真实 JSON 文件。测试控制上游边界，不访问真实账号或外网。

| 检查 | 命令/入口 | 结果 |
| --- | --- | --- |
| 首轮红绿 | `-m pytest test/test_account_retention.py -q` | 分别先复现 DELETE 200 且删除、自动限流删除、旧配置公开输出、搜索失效健康未更新，再修复通过 |
| 审查红绿 | `-m pytest test/test_account_retention.py -k 'search_polling or http_auth' -q --tb=short` | 搜索轮询 429 和文本/图片 401/429 四组合先失败；修复后全部通过 |
| 最终本票 | `-m pytest test/test_account_retention.py -q` | 8 passed，4 subtests passed；Spec 审查者独立复跑通过 |
| 账号阶段回归 | `-m pytest test/test_account_retention.py test/test_account_usage_modes.py test/test_account_image_capabilities.py test/test_model_catalog_service.py -q` | 扩展审查前 34 passed |
| 审查修复相关回归 | `-m pytest test/test_account_retention.py test/test_account_usage_modes.py test/test_text_model_routing.py -q --tb=short` | 27 passed，4 subtests passed |
| 最终完整离线套件 | `.scratch/image-workflow-upgrade-delivery/reports/17-offline-check.py` | **151 passed，5 个既有失败，4 subtests passed**，退出 1 |
| TypeScript | web 目录：`C:/Program Files/nodejs/node.exe node_modules/typescript/bin/tsc --noEmit --pretty false` | 退出 0 |
| 生产构建 | web 目录：`C:/Program Files/nodejs/node.exe node_modules/next/dist/bin/next build` | 退出 0，11 个静态页面生成 |
| 差异检查 | `git diff --check` | 通过 |

依赖只安装已有 package.json 内容，使用 `npm install --ignore-scripts --no-package-lock --cache .npm-cache --fetch-retries=0`，没有新增依赖或锁文件。安装尚未完成时一次 tsc 报 Next 声明文件缺失，下载完成后正式检查通过；未用声明补丁绕过类型检查。缓存移入本工作树忽略的 `.venv/`。

完整套件沿用 VALIDATION.md 的手动联调排除清单，`test_v1_models` 仅执行两个指定离线断言。旧远程图片测试的下载边界受控失败，避免联网。五项基线失败保持原责任划分：

- `test_poll_waits_for_generated_asset_ids_to_settle`：交票05。
- `test_image_edit_accepts_json_image_url`、`test_image_edit_accepts_json_multiple_images_and_b64_json`、`test_image_edit_rejects_json_without_image`、`test_image_edit_rejects_remote_json_url`：交票04。

未扩大本票去修这些基线；不能把本报告解读为整体验收已全绿。

## 浏览器证据

使用 [17-browser-server.py](17-browser-server.py) 在 43271 启动真实账号/设置/鉴权 API，临时 JSON 账号、旧开关 true、受控额度刷新；43270 的 `python -m http.server 43270 --bind 127.0.0.1 --directory web/out` 提供生产导出页面。

运行 `C:/Program Files/nodejs/node.exe .scratch/image-workflow-upgrade-delivery/reports/17-browser-check.cjs`：退出 0。独立无头 Chrome context 拦截外网并将 API 请求转发到受控本地服务，实际完成登录、选择账号、修改用途、刷新全部账号、重载与设置页检查。1440×900 和 390×844 均保留两条账号，删除入口 0、旧开关 0、pageerror 0。脚本和 [结构化结果](17-evidence/results.json) 可复核。

- [桌面列表](17-evidence/accounts-1440.png)、[桌面刷新后](17-evidence/after-refresh-1440.png)、[桌面设置](17-evidence/settings-1440.png)
- [窄屏列表](17-evidence/accounts-390.png)、[窄屏刷新后](17-evidence/after-refresh-390.png)、[窄屏设置](17-evidence/settings-390.png)

已人工查看列表及设置截图。浏览器证据采自 1c35397；随后 8e591aa 只修后端异常路径，最终离线套件已覆盖。服务以隐藏窗口启动；通过 PID/命令核对后停止本票父进程 30816、34116 和实际 Python 服务 19484、28272，确认均已退出。

## Standards

独立子代理 `/root/review_standards` 审查 `30785289...1c35397`，再复审 `30785289...8e591aa`：**零行动项**。未发现文档硬性违规或值得修改的 smell；共用限流更新和失效分类符合最小实现原则。

## Spec

独立子代理 `/root/review_spec` 首轮发现 P2：文本/图片普通 HTTP 401、429 未更新健康。主对话另发现搜索轮询吞掉 429。均已有公开边界失败复现、修复与新增回归；复审 `30785289...8e591aa`：**零剩余行动项**，P2 已闭环。审查者独立运行本票 8 项及 4 子场景通过。

最终 Standards 0、Spec 0。实现与修复提交须依次集成，报告/证据提交随后；无需额外迁移。账号隐藏、排序的合并上下文由主对话与票16联合核对。
