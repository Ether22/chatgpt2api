# 票 01：Prompt 折叠、图片顺序与数量记忆

本票功能验收通过，已完成实现、浏览器验证、类型检查、构建及双轴审查。完整离线自动化测试有 5 个既有后端失败，详见下文；未将其标为通过，也未扩大本票修改范围。

- 工作树：`C:/Users/ForestHill/.codex/worktrees/d9c8/chatgpt2api`
- 分支：`codex/image-task-01`
- 统一基线：`2e3cba11c3d6e85a0daba22b4d4292cecd95af4d`
- 待审实现提交：`78daecd`；后续提交仅完善验证脚本、截图和本报告，生产代码与该提交一致。
- 未执行 merge、rebase 或 cherry-pick；未编辑主任务的 PROGRESS.md、其他票、账号代码、`web/src/lib/api.ts` 或公共 Dialog。

## 修改范围

| 文件 | 修改 |
| --- | --- |
| `web/src/app/image/page.tsx` | 首次默认 4；集中校验纯数字 1–100 整数；非法值在创建轮次前被拒绝；仅用户的有效数量修改写入偏好，挂载时恢复，历史加载不再重设数量。 |
| `web/src/app/image/components/image-composer.tsx` | 删除数量快捷按钮，保留一个带标签、数字键盘提示、错误状态及范围说明的输入框；非法值禁用生成按钮。 |
| `web/src/app/image/components/image-results.tsx` | 长 Prompt 默认两行并支持展开/收起；轮次信息和参考图保持独立；以 CSS Grid 替代桌面多栏布局，维持原响应式列数并逐行排列。 |
| 本报告目录中的 `01-browser-server.py`、`01-browser-check.cjs`、`01-evidence/` | 可复跑的受控上游服务、浏览器行为断言和实际截图/日志。不增加项目依赖或测试框架。 |

数量偏好沿用已有 localStorage 键；显式“复用配置”仍会采用被复用轮次的数量。没有提前实施票 03 的服务器会话、MD 导入或其他后续能力。

## 逐项验收

| 需求 | 实际验证 | 结果 |
| --- | --- | --- |
| US1：长 Prompt 默认两行，可展开/收起 | 通过普通入口提交六行 Prompt；浏览器测量折叠高度不超过 2.1 行，展开至少六行，再收起恢复两行；短 Prompt 无多余展开按钮。 | 通过 |
| US2：折叠后参考图和任务标识仍可见 | 折叠后核对“第 1 轮”和参考图预览按钮；截图保留编辑图类型、状态和时间。 | 通过 |
| US3：响应式图片逐行 1、2、3、4 | 上游返回不同宽高比例且允许不同完成顺序的合成图；按真实图片按钮的屏幕坐标排序，1440px 为三列，1000px 为两列，600px/390px 为三列，均读作 1、2、3、4。 | 通过 |
| US29：首次默认 4 | 新建空浏览器上下文，从登录及普通生图入口进入；基线产物实际显示 1，修改后显示 4。 | 通过，已取得失败基线 |
| US30：纯数字 1–100，非法值不能提交 | `空字符串、0、101、-1、1.5、1e2、0x10、带空格的 4、abc、全角１２` 均禁用按钮；在文本框按 Enter 后保留草稿、显示错误且没有图片任务 HTTP 请求。分别实际提交 1 和 100，核对请求数量并确认末张成功结果可见。 | 通过 |
| US33：手动数量不被上传/重渲染/刷新重置 | 改为 7，上传合成参考 PNG，修改 Prompt 触发重渲染，再刷新页面，数量均保持 7。 | 通过 |
| 普通入口独立验收 | 全程走既有登录、普通 Prompt/参考图、生图结果和下载入口，没有等待或使用 MD 功能。 | 通过 |
| 下载回归 | 点击结果的下载按钮，收到 `image-1.png`，校验下载字节的 PNG 文件头。 | 通过 |

## 实际测试命令与结果

以下均在本工作树运行。前端 Node 来自共享工具运行时；最终 Python 使用主任务提供的 Python 3.13.15 冻结依赖解释器。共享解释器仅作为工具读取，没有读取主目录的运行配置或账号/图片数据。

```powershell
$node = 'C:/Users/ForestHill/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/bin/node.exe'
$python = 'C:/Users/ForestHill/Documents/chatgpt2api/.venv/runtime/Scripts/python.exe'

# 从工作树根目录：类型检查，退出 0（TypeScript 5.9.3）
& $node web/node_modules/typescript/bin/tsc --noEmit -p web/tsconfig.json

# 从 web/：生产构建，退出 0，11 个静态页面完成
& $node node_modules/next/dist/bin/next build --webpack

# 从工作树根目录：完整离线自动化，退出 1
$env:CHATGPT2API_AUTH_KEY = 'chatgpt2api'
& $python -m pytest -q test `
  --ignore=test/test_v1_chat_completions.py `
  --ignore=test/test_v1_images_edits.py `
  --ignore=test/test_v1_images_generations.py `
  --ignore=test/test_v1_messages.py `
  --ignore=test/test_v1_responses.py `
  -k 'not test_list_models_function and not test_list_models_http'
```

完整离线结果：**133 passed, 5 failed, 2 deselected**，耗时 12.92 秒，另有既有 SQLAlchemy 弃用警告。早期针对任务接口/服务的 `pytest -q test/test_image_tasks_api.py test/test_image_task_service.py` 为 **8 passed**；最终整套运行也包含这 8 项。Next 配置原本跳过构建内类型检查，因此单独运行并通过 `tsc --noEmit`，没有用构建成功代替类型检查。

5 个失败均位于未修改的后端/旧测试中；`git diff 2e3cba11c3d6e85a0daba22b4d4292cecd95af4d -- api services test` 为空：

1. `test_multi_image_results.py::MultiImageResultTests::test_poll_waits_for_generated_asset_ids_to_settle`：期望 `file-one,file-two`，实际仅 `file-one`。主任务已在原始基线复现，交后续票核查。
2. `test_v1_images_edits_json.py::ImageEditsJsonApiTests::test_image_edit_accepts_json_image_url`：旧测试期望 `image_1.png`，现有实现返回 `image_url.png`。
3. 同类 `test_image_edit_accepts_json_multiple_images_and_b64_json`：旧测试期望带序号文件名，现有实现返回 `image_url.png/jpg`。
4. 同类 `test_image_edit_rejects_json_without_image`：旧测试期望 `image file is required`，现有错误为 `image file or image_url is required`。
5. 同类 `test_image_edit_rejects_remote_json_url`：旧测试期望禁止远程 URL，现有实现尝试读取 URL，在离线沙箱中读取失败。

未执行的真实上游/人工联调：上述五个 `--ignore` 文件，以及 `test_v1_models.py` 中 `test_list_models_function`、`test_list_models_http`。另有 `main()` 手动脚本 `test_codex_4k.py`、`test_generations.py`、`test_generations_url.py`、`test_gpt_ppt.py`、`test_gpt_psd.py`、`test_gpt_search.py`、`test_image.py`、`test_image_output_tokens.py`；pytest 导入这些文件不会执行其手动入口。这些脚本需要运行服务、上游凭据/样本，不列为通过。

## 浏览器证据与复跑

服务将当前 `web/out` 作为静态普通页面提供，使用真实 FastAPI 路由、身份检查、图片任务服务和工作树内临时任务存储；只控制外部图片处理器和上游模型目录。没有引入服务器会话功能。

```powershell
# 工作树根目录；后台服务使用隐藏窗口，固定端口 43110
Start-Process -FilePath $python `
  -ArgumentList '.scratch/image-workflow-upgrade-delivery/reports/01-browser-server.py' `
  -WorkingDirectory 'C:/Users/ForestHill/.codex/worktrees/d9c8/chatgpt2api' `
  -WindowStyle Hidden

& $node .scratch/image-workflow-upgrade-delivery/reports/01-browser-check.cjs
```

服务内固定测试密钥为 `ticket01-browser-only`；测试使用新的无界面 Chrome 上下文，不复用用户浏览器账号，并阻止 `http://127.0.0.1:43110` 以外的所有请求。未使用其他任务端口。初始 CUA 浏览器附加失败后，按主任务/票 02 确认的方法使用共享 Playwright 和已安装 Chrome，最终浏览器检查退出 0。

- [完整浏览器通过日志](01-evidence/browser.log)
- [失败基线：首次实际 1 张](01-evidence/baseline-default-one.png)
- [修改后首次 4 张](01-evidence/initial.png)
- [非法数量状态](01-evidence/invalid-count.png)
- [1440px 三列与 Prompt/参考图](01-evidence/results-1440.png)
- [1000px 两列](01-evidence/results-1000.png)
- [600px 三列](01-evidence/results-600.png)
- [390px 窄屏](01-evidence/results-390.png)

最终受控场景从点击提交到末张成功图片可见：1 张为 **2503ms**，100 张为 **6161ms**。这是合成上游、少量历史下的端到端观测，包含现有轮询等待；不代表真实上游生成速度或后续数千张图库性能。截图已人工查看，配合真实几何位置断言确认布局，未使用截图快照测试来镜像实现。

## Standards

独立审查基线 `2e3cba1...78daecd`：**0 项**。未发现 AGENTS.md、issue-tracker、domain、CONTEXT 或两项 ADR 的违反；无值得行动的新增 smell。数量校验集中，折叠与布局修改局部，验证使用独立上下文和临时存储。该静态审查不代替上文实际浏览器结果。

## Spec

独立审查同一固定点：**0 项**。US1–3、US29、US30、US33 均已实现；未发现遗漏、错误实现或越界功能。没有改账号、公共 API、共享 Dialog 或服务器会话。

双轴汇总：Standards 0，Spec 0；无需要修改的行动项。

## 剩余限制

- 5 个既有后端测试失败需由主任务/后续票处置，本票没有修复或隐藏它们。
- 未进行有凭据的真实上游联调；所有浏览器图片为合成数据。
- 本票只覆盖普通生图的可读结果和数量偏好；服务器历史、并发、MD、导航和大图库优化继续由对应后续票承担。
