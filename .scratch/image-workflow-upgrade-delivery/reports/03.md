# 03：服务器生图会话与轮次闭环

工作树：`C:/Users/ForestHill/.codex/worktrees/01e3/chatgpt2api`。分支：`codex/image-task-03`。派发／审查固定点：`d20ae3360ec5852a7d1160356669e2a8d8f63857`。当前为待独立审查版本；主任务集成前会补齐最终结果。

## 范围与行为

- 扩展现有 `ImageTaskService` JSON 快照，同一次原子保存涵盖当前会话、来源条目、不可变轮次配置和全部单图任务归属；保存成功后才启动消费。并发首次提交在既有服务锁内选定同一身份的当前会话，`request_id` 保证重试不重复创建轮次或任务。
- 原子文件保存改为唯一临时文件、flush/fsync 和有界 Windows PermissionError 重试，任务与图片索引共用实现。提交失败回滚内存；初次 running 保存进入异常处理，持续磁盘故障保留真实错误供实时读取，重启保留已落盘的中断状态。
- 已知磁盘容量／目录不可写、图片索引不可写和 WebDAV 探测失败在消费前拒绝。受管理图片、缩略图及任务不参加自动过期和低磁盘清理。
- 新图片位于稳定登录身份对应的 managed 路径；原图、缩略图、任务、会话、图库、下载和标签验证归属，管理员身份也不自动拥有其他身份的结果。旧图库删除和压缩不会绕过受管理历史关系；完整物理删除仍属10/11。
- 页面直接提交完整轮次、读取服务器历史；删除 IndexedDB 历史迁移、逐图提交、客户端消费队列和全量 Base64 回写。浏览器只保存偏好及临时图片 Blob。受保护图片显示／下载／参考图复用携带 Authorization；图库两处预览也复用该能力。
- 未改 `web/src/lib/api.ts`、账号 API/服务、`protocol/conversation.py`，保留15资格逻辑。经主任务协调新增 `services/image_tags_service.py` 的现有函数归属筛选及图库局部图片适配。`config.py` 只修改图片保留区域。

## 验收证据

本票 HTTP 检查：`test/test_image_conversations_http.py`，真实临时任务／图片／标签／身份存储，仅控制上游与 WebDAV 外部边界。最近一轮21项通过：

- 同身份独立客户端及重载服务恢复当前会话、配置、来源、轮次、任务和图片；其他身份不能读／写。
- 八个并发首次请求（四个逻辑提交各重试一次）只有一个目标、四个轮次、四次消费。
- 提交保存永久失败不留下可见会话或任务，零消费；解除故障后同一请求可成功。
- running 状态保存失败不调用上游、任务进入持久错误；真实 Windows CreateFileW 共享锁触发 WinError5/32 后有界重试成功，只有一次消费。
- 普通1／4／100张均按请求数量全部成功，没有放宽缺图断言。
- 原图／缩略图、旧图库／下载／标签的跨身份旁路被拒绝；managed 的 Windows 尾点／空格路径别名被拒绝。
- 过期文件和低磁盘清理保留 managed；已知空间不足、索引不可写、远端不可写均在消费前拒绝。
- WebDAV／双存储的真实合成图片通过应用鉴权路径读取，不向图库或任务暴露远端／公共 CDN 原始地址。
- 普通参考图入口可提交并恢复；畸形 data URL 在 HTTP 边界拒绝。独立文件引用和快照由04扩展。

独立 TypeScript：`node node_modules/typescript/bin/tsc --noEmit --pretty false`，通过。生产构建：`npm run build`，通过，11个静态页面完成；没有把 Next 跳过类型检查当成 tsc。

生产浏览器检查：`03-browser-server.py` ＋ `03-browser-check.cjs`。使用43130、独立 Chrome contexts、外网请求封锁、假身份／假账号／合成图片；不运行真实 OAuth 或上游消费。已通过：

- 首次默认4，旧 IndexedDB 内容不迁移，额度仅计 normal＋健康正常账号。
- 1440／1000／600／390四种宽度保持1、2、3、4的阅读顺序；Prompt 折叠／展开正常。
- 新浏览器以同一身份恢复当前会话和原图；新建空会话及1张／100张提交正常。
- 1张约1917ms，100张全部成功约7626ms；处理期间输入不被保存响应清空。
- 原图下载得到 PNG；管理员图库的 managed 缩略图通过授权请求可见。
- 同浏览器退出A、登录B，没有A的历史，旧 Blob URL 已失效，B直接请求A的原图被拒绝；B的普通参考图入口正常。
- 4个整轮 POST，51个页面保护图片请求全部带授权，零 pageerror。机器结果见 `03-evidence/browser-result.json`；截图见同目录。

额外真实根因：100图曾发现 `_local_image_path` 将 Windows 长路径 `\\?\C:...` 与无前缀根目录比较，误报越界404。已统一命名空间比较并复用原图安全路径。此问题与 tasks.json 共享替换失败分别记录，不假定杀毒软件是根因。

## 后续票的接口

- `GET /api/image-conversations` → `{items, current_conversation_id}`；`GET /api/image-conversations/{id}` → 当前身份的会话。
- `POST /api/image-conversations` 使用 `request_id` 显式新建；`PUT /api/image-conversations/current` 保存当前会话选择。
- `POST /api/image-conversations/turns` 使用 `request_id`、可选 `conversation_id`／`source_entry_id`，以及 Prompt、模型、尺寸、画质、数量、ratio/tier 和当前兼容的 referenceImages。服务器产生会话 ID、`sourceEntryId`、轮次 ID、单图 `taskId`。
- 内部任务用 `image_conversation_id`／`turn_id`／`source_entry_id` 区别于已有上游 `conversation_id`，避免混淆。
- 04可把轮次中的兼容内联参考图替换为文件引用／独立快照；13可在当前读接口上增加分页和轻量导航。05接续恢复／账号等待调度；10/11接续引用释放、物理删除与迟到清理。

## 明确限制

- 当前仍为既有单进程锁＋JSON快照模式；大历史分页、数据库式增量记录和多进程写入不在03交付范围，代码保留 ponytail 上限说明。
- 完整队列重启续跑属05；本票不重发未知已消费任务，保留中断状态。消费后的永久存储故障不能保证磁盘记录被更新，实时状态保留真实保存错误。
- 会话／轮次展示删除仅持久化隐藏状态，未实现10/11的物理删除与远端清理；原文件保留，不宣称已释放存储。
- 业务日期由18统一，本票没有改写15消费资格或其他票配置区域。

## Standards

待独立审查。

## Spec

待独立审查。
