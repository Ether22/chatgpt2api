# 票 15：监控、禁用与可消费额度

## 交付范围

- 工作树：`C:/Users/ForestHill/.codex/worktrees/0dd2/chatgpt2api`
- 分支：`codex/image-task-15`
- 派发/审查固定点：`2e3cba11c3d6e85a0daba22b4d4292cecd95af4d`
- 代码提交：`43e3834`（替代本地待审提交 `0ec7a14`，已包含审查修复）。本报告和截图另附报告提交；最终提交号见交付消息。
- 未改主目录应用文件、唯一 PROGRESS、其他票、导入弹窗或生图页面；未自行集成其他分支。

新增持久字段 `usage_mode = normal | monitor | disabled`，与上游健康 `status` 分离。账号 API 校验三种互斥值；缺省为正常使用，无法识别的存储输入按禁用处理，避免意外消费。没有增加存储格式迁移或依赖。

消费资格集中在账号服务。图片分配、文本分配及失效重试、两个搜索入口、PPT/PSD 选择器与模型能力判断统一排除监控/禁用。刷新令牌后再次核对账号存在且用途允许消费。后台 watcher 继续维护两类账号，包括健康状态异常/停用时；额度刷新结果不能覆盖用途，令牌轮换保留用途。旧自动移除规则仅对本票两类账号增加保护，正常账号的全面删除改造留票 17。

现有编辑弹窗提供三种用途选择，列表监控组置顶，并显示背景、色条、图标和原生悬停说明。监控额度单列，监控/禁用均不计入可用剩余额度。移除刷新期间不准确的部分结果推算，统计保留最近完整快照并在刷新结束后更新；保存用途后更新模型列表。隐藏与组内拖拽排序留票 16。

修改文件：`api/accounts.py`、`api/support.py`、`services/account_service.py`、`services/editable_file_task_service.py`、`services/model_service.py`、`services/protocol/openai_v1_models.py`、`services/protocol/conversation.py`（仅文本重试）、`web/src/app/accounts/page.tsx`、`web/src/lib/api.ts`、`test/test_account_usage_modes.py`。

## 逐项验收

| 本票要求 | 结果与证据 |
| --- | --- |
| 三种用途互斥，后台/手动刷新不覆盖 | 通过。HTTP 保存后以新服务实例读取临时真实 JSON；非法组合返回 422、无凭据修改返回 401。浏览器保存、重载及手动刷新均保持用途。 |
| 监控/禁用继续刷新、监测、保活 | 通过。真实 watcher 在受控上游下维护上游异常/停用的两类账号，更新健康和额度，OAuth 轮换令牌后仍保持用途。自动移除开关开启时，限流、失效与迟到图片结果不删除它们。 |
| 所有消费及能力判断排除两类账号 | 通过。公开服务行为覆盖图片、文本、两个搜索、PPT/PSD；额度不足时图片分配明确失败，文本保留原有匿名回退。模型缓存随资格集合变化更新；刷新期间改变用途/账号消失，以及失效重试期间改为监控或禁用均有回归验证。 |
| 额度准确分列 | 通过。正常额度 3、监控额度 11、禁用额度 17 时可用额度为 3，监控为 11；补充限流监控额度 5 后监控合计 16，可用仍为 3。服务统计、刷新进度及界面均排除非消费用途。 |
| 监控组置顶并具备四种提示 | 通过。监控账号在最后导入仍显示首行；背景、左侧色条、眼睛图标与悬停说明均存在。 |
| 设置留在现有编辑界面 | 通过。没有行监控开关或批量监控入口；桌面及 390×844 窄屏的用途、保存和取消均可达。 |

## 测试命令与结果

以下命令均在本工作树执行。共用解释器只读取主对话准备的 Python 3.13.15 环境；代码、配置、账号和图片均来自本工作树或临时目录，没有读取主目录运行配置/真实数据。

```powershell
$py = 'C:/Users/ForestHill/Documents/chatgpt2api/.venv/runtime/Scripts/python.exe'
$env:CHATGPT2API_AUTH_KEY = 'ticket-15-test'
& $py -m unittest test.test_account_usage_modes -v
& $py -m unittest test.test_account_usage_modes test.test_text_model_routing -v
```

本票 10 项通过；与文本路由组合 19 项通过。早期账号能力、模型目录、路由组合 34 项通过；之后新增的竞态与重试回归均纳入最终整套。

逐切片先得到失败再实现：API 忽略用途返回 400；文本轮询选中 monitor/disabled；用途改变后仍公布 Pro/图片能力；watcher 只维护正常账号；可用额度错误为 31 而非 3；限流监控账号被自动删除；PPT/PSD 使用监控账号；刷新期间改变用途仍分配新令牌；账号消失仍返回旧令牌；文本重试消费两类已改用途的轮换令牌。以上失败均已转为通过，未把测试装配错误计作功能失败证据。

完整离线自动化采用现有 unittest/pytest 用例，保留 `test_v1_models.py` 的两个受控用例，排除实际服务联调：

```powershell
$env:CHATGPT2API_AUTH_KEY = 'chatgpt2api' # 现有离线接口用例的虚构固定密钥
$offlineFiles = @(rg -l 'unittest.TestCase|^def test_' test -g '*.py' |
  Where-Object { $_ -notmatch 'test_v1_(chat_completions|images_edits\.py|images_generations|messages|responses|models)' })
& $py -m pytest @offlineFiles `
  test/test_v1_models.py::ModelListTests::test_list_models_only_returns_image_models_backed_by_account_types `
  test/test_v1_models.py::ModelListTests::test_list_models_does_not_return_codex_models_for_web_plus_accounts `
  -q --tb=short
```

最终结果：**148 项，143 通过、5 失败，12.66 秒**。5 项均与主对话确认的原始基线失败一致，本票没有新增失败：

1. `test_multi_image_results.py::MultiImageResultTests::test_poll_waits_for_generated_asset_ids_to_settle`：图片轮询未等待资源集合稳定，只取 `file-one`，由票 05 后续核查。
2. `test_v1_images_edits_json.py::ImageEditsJsonApiTests::test_image_edit_accepts_json_image_url`：旧测试期待 `image_1.png`，当前实现返回 `image_url.png`。
3. 同类 `test_image_edit_accepts_json_multiple_images_and_b64_json`：同样为默认文件名差异。
4. 同类 `test_image_edit_rejects_json_without_image`：旧缺图文案断言与当前文案不一致。
5. 同类 `test_image_edit_rejects_remote_json_url`：旧测试期待拒绝远程 URL，当前实现尝试读取；沙箱阻断 example.com 请求后返回获取失败。后四项由票 04 后续核查。另有一条既有 SQLAlchemy deprecated API 警告。

前端在 `web` 目录执行：

```powershell
npm.cmd install --ignore-scripts --no-package-lock
node node_modules/typescript/bin/tsc --noEmit
node node_modules/next/dist/bin/next build
```

类型检查通过；生产构建通过，11 个静态页面完成生成。项目构建配置跳过内部类型检查，故另行执行了上面的 `tsc`。`git diff --check` 通过。只安装项目既有前端依赖，未提交 node_modules、安装缓存或锁文件。

未执行、也不宣称通过的真实服务/凭据联调：`test_v1_chat_completions.py`、`test_v1_messages.py`、`test_v1_responses.py`、`test_v1_images_generations.py`、`test_v1_images_edits.py`，以及 `test_v1_models.py` 的 `test_list_models_function` / `test_list_models_http`。未执行的手动上游脚本为 `test_codex_4k.py`、`test_generations_url.py`、`test_generations.py`、`test_gpt_ppt.py`、`test_gpt_psd.py`、`test_gpt_search.py`、`test_image_output_tokens.py`、`test_image.py`；`scripts/verify_oauth_refresh.py`、`scripts/test_outlook_token_mailbox.py`、`scripts/test_storage.py` 亦未运行。

## 浏览器证据

使用独立 Codex 浏览器标签；前端 `127.0.0.1:43250`、受控 FastAPI `127.0.0.1:43251`。三个虚构账号为 `qa-normal`、`qa-monitor`、`qa-disabled`，邮箱使用 `example.test`。账号读写与持久化采用实际 API/服务；上游额度、模型和 OAuth 以受控行为验证，不发送真实消费。

测试期间临时将开发 API 地址改为 43251，验收后已恢复原文件；临时服务与子进程已停止，标签已关闭，视口覆盖已撤销。临时 QA 脚本、数据和日志留在忽略目录，不随代码交付。

- [监控置顶、可用 3 / 监控 11](15-evidence/accounts-monitor.png)
- [现有编辑弹窗的三种互斥选项](15-evidence/usage-options.png)
- [恢复正常后可用额度 14、Pro 图片能力恢复](15-evidence/normal-capabilities.png)
- [重新设为监控后刷新，额度和用途保持](15-evidence/after-refresh.png)
- [390×844 窄屏的编辑、保存和取消](15-evidence/narrow-edit.png)

没有把浏览器冷编译耗时当作产品性能结论。本票未改下载逻辑或大型图库，未声称验证其性能。

## Standards

独立只读审查固定命令 `git diff 2e3cba11c3d6e85a0daba22b4d4292cecd95af4d...HEAD`，初审 `0ec7a14`、修复后复审 `43e3834`：**0 项行动项**。未发现违反 AGENTS、领域文档规则或 ADR 的变更；用途与上游健康分离，资格集中复用，没有新增依赖或无需求抽象。

## Spec

初审发现 **1 项 P1**：文本失效后直接重试刷新令牌，可能消费刚被设为监控/禁用的账号。已将所有文本重试重新送入共享选择器，并用临时真实存储、受控 OAuth 回调和 SDK 文本流分别复现、修复两种用途。复审 `43e3834` 确认原 P1 关闭，另独立运行三项相关回归通过，**剩余 0 项行动项**。

主对话额外指出的“空账号误判可用”问题，也已通过共享非空守卫和刷新期间账号消失回归修复。

## 剩余边界

本票没有未解决行动项。五项离线基线失败如上；真实凭据联调没有执行。隐藏、组内顺序持久化/拖拽留票 16，全面取消账号物理删除留票 17，因此本票截图仍含既有删除入口。主对话负责最终审查、集成和唯一进度表更新。
