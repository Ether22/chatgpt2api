# 11：范围删除、迟到清理与批量性能

**状态：implemented-awaiting-main。** 票 11 实现、离线验收、生产浏览器验证和双轴复审均完成，交主任务集成。

- 工作树 `C:/Users/ForestHill/.codex/worktrees/06d3/chatgpt2api`，分支 `codex/image-task-11`。
- 固定基线 `8dc5aaa91533199d6daf2cf7eb25a84e7963cf7b`；实际派发模型/强度 `gpt-6-astra / high`。
- 实现提交 `39799321be02a702bed59c153b09cd9a941ead2d`；审查修复提交 `9da2fb76111501bce254dc701384897bcf1a1a89`。最后的报告/证据提交以交付消息及本分支 HEAD 为准。

## 实现与验收对应

| 验收项 | 实现及结果 |
| --- | --- |
| 本轮、单会话、身份全部历史范围正确 | 旧 PATCH `resultsDeleted` 接真实删除；会话和身份范围删除同事务保存任务墓碑与图库不可见性；确认框说明范围，删除不清空当前共享导入素材。双身份浏览器及 HTTP 均通过。 |
| 未启动不消费，迟到不复活 | 发送事件与删除共用任务锁；真实账号池等待者在发送前取消，已发送/未知发送任务保留回执并继续隐藏清理。真实受控 HTTP、晚到 Base64 和协议 URL 两支均验证。 |
| 引用保护和失败重试 | 沿用 tasks/references 持有关系；共享引用保留，已成功清理的副本不重复删除。删除后的会话返回 404，身份级清理列表仍可查看错误和重试，服务重启后可继续。 |
| 批量清理 | 本地每批 64 路径，远端最多 4 并发；SQLite 批量写入变化行，标签按批更新。3200 张真实 PNG 清理后原图、缩略图和标签均消失。 |
| 页面立即反馈与旧响应屏障 | 本轮和范围删除立即隐藏；请求序号/进行中屏障抑制旧详情和分页响应。跨浏览器删除、已选会话不在首页、自动切换会话和同轮删除均关闭失效结果查看器。参考图查看不受影响。 |
| 保持手动历史清理 | 仅显式删除触发清理；没有新增历史 TTL 或自动淘汰。 |

## 接口和持久化边界

- 保留原单图路由，新增身份隔离的 `GET /api/image-cleanups`（每页 50）和 `POST /api/image-cleanups/{task_id}/retry`。UI 常驻“清理状态”入口，展示进行中、错误及重试。
- 复用同一 task 行的 `result_deleted`、`result_cleanup`，增加 `result_storage_paths` 写入意图；不增加第二套任务或持有关系表。`image_rows.save_deletions` 同一事务保存任务和索引变更。
- `storage.owner_scope` 的可选 `before_save` 回调在写入副本前保存路径和索引意图。即使迟到结果已写文件、后续 task data 的真实 SQLite 提交失败，重启仍能找到路径并重试清理；协议 URL 和服务 Base64 路径均覆盖。结果永久删除不能通过 `resultsDeleted=false` 恢复，Prompt 显隐仍可恢复。
- 主任务划分的票 08 `submit_turn`、`_prepare_payload`、`upload_reference`、`_start_task` 区域未修改。08+11 的 reserved/pending/ready/failed 引用组合由主任务最终集成验证；本票在固定基线的实际接口上验收。
- 全局清理协调锁保留并以 `ponytail:` 注释写明吞吐上限和升级条件；远端四并发只控制删除副本。未新增依赖。

## 测试与复现

全部测试从本工作树运行，使用假账号、临时 SQLite/PNG 和受控上游/远端存储；真实上游消费 **0**。只借用既有 Python、Node 和浏览器运行时，未导入主目录真实 data/config。

| 检查 | 结果 |
| --- | --- |
| 新增 `test/test_image_scope_deletion.py` 公共 HTTP 专项 | 11 项通过，包括真实池等待取消、共享引用、身份隔离、迟到、提交/文件删除故障及重启重试。 |
| 范围删除与单图组合 | 29 项通过；此前范围/单图/恢复组合 39 项通过。 |
| 批准的完整离线套件 | **293 passed, 7 subtests passed，61.47 秒**；仅既有 anyio 警告。 |
| 独立 TypeScript | `tsc --noEmit --pretty false` 通过。 |
| Next 生产构建 | 通过，11 个静态页面。 |
| 生产浏览器完整脚本 | 通过，真实 PNG 下载、双独立登录、全部删除范围、旧响应、远端部分失败与重启重试、390×700 窄屏、3200 原图，页面异常 0。 |
| 旧查看器回归 | 两个真实红灯场景复现后修复，`11-viewer-check.cjs` 绿灯；覆盖删除后切到其他会话及同轮删除。 |

保留的两处旧测试调整有规格依据：引用测试的受控上游补实际 `sending` 事件，原引用保护断言保持；分页测试把“恢复已删结果”改成永久 404，并保留 Prompt true/false 导航恢复断言。首次完整套件由旧恢复断言失败，更新后完整重跑全绿。

PowerShell 复现命令（当前目录为本工作树；前端命令从 `web` 运行）：

```powershell
& 'C:/Users/ForestHill/Documents/chatgpt2api/.venv/runtime/Scripts/python.exe' .scratch/image-workflow-upgrade-delivery/reports/17-offline-check.py
& 'C:/Program Files/nodejs/node.exe' node_modules/typescript/bin/tsc --noEmit --pretty false
& 'C:/Program Files/nodejs/node.exe' node_modules/next/dist/bin/next build
```

浏览器验证先在本工作树启动 `11-browser-server.py`，再运行两个 Node 脚本（服务仅绑定 `127.0.0.1:43210`，浏览器脚本限制请求为该同源）：

```powershell
& 'C:/Users/ForestHill/Documents/chatgpt2api/.venv/runtime/Scripts/python.exe' .scratch/image-workflow-upgrade-delivery/reports/11-browser-server.py
& 'C:/Program Files/nodejs/node.exe' .scratch/image-workflow-upgrade-delivery/reports/11-browser-check.cjs
& 'C:/Program Files/nodejs/node.exe' .scratch/image-workflow-upgrade-delivery/reports/11-viewer-check.cjs
```

浏览器服务需单独终端运行；后台启动时使用 `Start-Process -WindowStyle Hidden`。本次全部临时服务已停止，43210/43211 无监听；测试运行时进程已退出。

## 3200 张真实 PNG 的最终测量

每张原图使用独立路径、480×320 PNG，另有两种缩略图；不是只造元数据。原图 3200、缩略图 6400 及对应标签全部清理，其他身份图片和当前参考图合计 2 张保留。

| 指标 | 实测 |
| --- | ---: |
| 本轮删除 UI 隐藏 | 75 ms |
| 慢速远端删除 UI 隐藏 | 72 ms |
| 3200 张范围删除 UI 隐藏 | 108 ms |
| 3200 张删除 HTTP 返回 | 1308 ms |
| 点击到完成观测 | 17537 ms |
| 后端物理清理阶段 | 16158.9 ms |
| 持有关系构建 / 目录扫描 | 2 / 0 |
| SQLite 写入行 / 删除行 | 16002 / 3201 |
| SQLite 序列化参数字节 | 9754305 |
| 标签写次数 / 字节 | 50 / 10916284 |

字节指标是实际序列化 SQLite 参数和标签内容大小，不等同于操作系统磁盘 I/O。最终数据见 [browser.json](11-evidence/browser.json)。视觉证据：[3200 完成](11-evidence/3200-complete.png)、[桌面范围删除](11-evidence/scope-deleted-desktop.png)、[远端等待](11-evidence/remote-pending.png)、[窄屏失败重试](11-evidence/remote-failure-narrow.png)、[迟到完成](11-evidence/late-cleaned-narrow.png)、[迟到提交故障](11-evidence/late-checkpoint-failure.png)、[旧查看器关闭](11-evidence/deleted-viewer-closed.png)、[实际下载 PNG](11-evidence/image-1.png)。

## Standards

独立子代理审查固定基线至 `9da2fb76111501bce254dc701384897bcf1a1a89`。最初 1 个 P3：全局协调锁的 `ponytail:` 注释未写上限与升级条件；已补齐。复审 **残留 0 项**，未发现新增规范违反或有实质影响的 code smell。规范来源包含 AGENTS、docs/agents、CONTEXT、ADR、pyproject 和技能要求的 Fowler 基线。复审为只读差异核验，不冒充重复执行测试。

## Spec

独立子代理以票 11 和父规格审查同一固定差异。最初 1 个 P2：跨浏览器删除后旧查看器可能仍显示被删除结果；真实浏览器红灯复现并修复，另补同轮删除回归。复审 **残留 0 个行动项**；分页响应屏障、永久结果删除与 Prompt 可恢复均符合规格。复审只读，实际浏览器和离线验证由实施任务执行。

两轴最终发现：Standards 0；Spec 0。实现已提交本地分支，未执行 merge、rebase、cherry-pick 或 push；主任务负责最终集成。
