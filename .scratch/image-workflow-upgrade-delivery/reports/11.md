# 11：范围删除、迟到清理与批量性能

实施中；尚未提交验收完成。

- 工作树 `C:/Users/ForestHill/.codex/worktrees/06d3/chatgpt2api`，分支 `codex/image-task-11`，基线 `8dc5aaa91533199d6daf2cf7eb25a84e7963cf7b`。
- 实际模型/强度：派发配置 `gpt-6-astra / high`。范围遵照主任务授权，不修改08提交、准备、引用预约或 `_start_task`。
- 旧PATCH `resultsDeleted` 已接真实删除；会话/身份清空同事务保存task墓碑和图库不可见性。`GET /api/image-cleanups`、`POST /api/image-cleanups/{task_id}/retry`保留已删除会话的进度与重试入口。
- 发送前取消共享任务锁；已发送结果继续落地清理。storage.owner_scope可选回调先持久保存原task行 `result_storage_paths` 及现有index写入意图，再写任何副本；覆盖真实协议先保存URL和服务保存Base64两支，防止task data写失败留下可见孤儿。
- 本地64路径一批、远端4并发；任务/索引变化行批量事务，标签按批更新；依旧仅tasks/references持有关系。
- 已通过：票11公开HTTP专项10项；此前专项+单图+恢复组合39项；独立tsc与11页生产构建。存储/引用/公开接口组合初跑49通过、1个旧夹具失败：旧“在途”受控handler没有sending事件，新取消逻辑按未发送正确停止；补实际sending事件后原引用保护断言通过，未削弱断言。
- 待完成：完整离线、生产浏览器所有范围/竞态与3200真实PNG性能、双轴审查、提交。最终报告将以真实命令、数据和SHA替换本中间状态。
