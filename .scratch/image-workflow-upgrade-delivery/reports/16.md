# 16：账号隐藏与组内持久排序

已在独立工作树 `C:/Users/ForestHill/.codex/worktrees/9924/chatgpt2api`、分支 `codex/image-task-16` 完成本票。基线及双轴审查固定点为 `d20ae3360ec5852a7d1160356669e2a8d8f63857`，实施提交 `6b9f716`。未自行集成其他票，未修改主目录应用代码、实际账号/图片或唯一进度表。

## 范围与行为

- `services/account_service.py`：现有账号 JSON 记录增加 `hidden`、`display_order`；兼容无字段的旧记录，JSON 与数据库均沿用既有存储。服务列表按监控组在前、组内保存位置排序；同组移动在锁内校验，保存失败恢复内存中的原顺序。用途切组追加到目标组末尾，正常与禁用互切仍属同组，不自动移动。
- `api/accounts.py`、`web/src/lib/api.ts`：现有管理员编辑接口支持隐藏；新增管理员 `/api/accounts/move`，提交源账号、目标账号及前/后位置。服务端拒绝跨组、缺失账号和非法位置，不信任客户端分组判断。
- `web/src/app/accounts/page.tsx`：默认过滤隐藏项，提供“显示隐藏账号”和逐行取消隐藏；完整账户数组继续用于统计和导出。原生拖拽排序柄支持目标行上下半部分别插入前/后；聚焦后 ↑/↓ 在组内移动，同页保存后保留焦点。键盘相邻移动可跨分页边界，但原行离开当前页后需切到目标页重新聚焦；本次5账号浏览器验收只覆盖同页移动。隐藏账号和未显示的筛选项不会从服务端顺序中丢失。
- 仅增加本票测试和证据。未新增依赖、未改变导入弹窗、图片域、用途消费规则或删除语义。

## 逐项验收

| 要求 | 证据与结果 |
| --- | --- |
| US100、107：隐藏独立于正常/监控/禁用，默认不显示并可找回 | HTTP 对三种用途分别保存隐藏并从真实 JSON 重新加载；浏览器默认只显示4/5账号，勾选显示隐藏并找回，刷新仍正确。通过。 |
| US108：隐藏不影响消费、额度刷新、统计 | 隐藏正常账号仍被文本和图片分配；正常/监控/禁用隐藏组合仍接受受控上游额度刷新，隐藏前后统计相同。通过。 |
| US109：原有导出规则不变 | 服务导出仍包含符合凭据规则的隐藏账号；浏览器“导出全部 Token”的实际下载文件包含默认隐藏的 `qa-b`。通过。 |
| US105：监控组在前，只能组内排序 | 两组分别移动持久保存；HTTP跨组400、缺失目标400、非法位置422、未鉴权401；浏览器跨组拖拽明确提示并不重排。通过。 |
| 重开、刷新、令牌轮换不重排；找回按保存顺序 | 真实JSON/SQLite重载，受控OAuth令牌轮换，随后额度刷新；浏览器先隐藏、轮换、重建服务对象，再找回，位置保持。独立浏览器context重新登录读到相同顺序。通过。 |
| 用途切组、键盘与窄屏 | 现有编辑框将禁用账号改为监控，追加到监控组并刷新保留；桌面与390px页面方向键移动有效，焦点保留；窄屏横向滚动后隐藏/找回按钮可到达并完成实际保存。通过。 |

## 可复跑检查与结果

从本工作树根目录执行，解释器只读取共用环境，不安装Python依赖：

```powershell
$py = 'C:/Users/ForestHill/Documents/chatgpt2api/.venv/runtime/Scripts/python.exe'
$env:CHATGPT2API_AUTH_KEY = 'chatgpt2api'
& $py -m pytest test/test_account_visibility_order.py test/test_account_usage_modes.py -q
```

本票4项通过，与票15组合最终14项通过；完整离线套件也包含这14项。TDD的功能失败依次为：隐藏字段被旧API忽略而返回400；移动端点不存在返回404；用途切组保留旧位置导致顺序错误。均先复现再实现修复。一次编辑位置错误引起的语法错误已立即修正，不计作功能失败证据。

完整离线命令：

```powershell
$env:CHATGPT2API_AUTH_KEY = 'chatgpt2api'
$offlineFiles = @(rg -l 'unittest.TestCase|^def test_' test -g '*.py' |
  Where-Object { $_ -notmatch 'test_v1_(chat_completions|images_edits\.py|images_generations|messages|responses|models)' })
$env:TICKET16_TEST_FILES = $offlineFiles -join '|'
@'
import os
from unittest.mock import patch
import pytest
files = os.environ['TICKET16_TEST_FILES'].split('|')
files += [
    'test/test_v1_models.py::ModelListTests::test_list_models_only_returns_image_models_backed_by_account_types',
    'test/test_v1_models.py::ModelListTests::test_list_models_does_not_return_codex_models_for_web_plus_accounts',
]
with patch('curl_cffi.requests.get', side_effect=RuntimeError('offline test: remote reads blocked')):
    raise SystemExit(pytest.main(files + ['-q', '--tb=short']))
'@ | & $py -
```

结果：**152项，147通过、5失败，14.70秒**。外部图片读取在HTTP客户端边界明确阻断，未访问真实上游。5项均为已确认基线失败：`test_v1_images_edits_json.py` 的两个默认图片文件名断言、缺图错误文案断言、远程URL旧拒绝断言，以及 `test_multi_image_results.py::test_poll_waits_for_generated_asset_ids_to_settle`。分别由票04、05处理。存在一条既有SQLAlchemy弃用警告；没有隐藏或改写失败结果。

未执行 VALIDATION.md 中真实HTTP/消费联调脚本；未运行真实OAuth或读取主目录账号图片。

前端在 `web` 目录：

```powershell
& 'C:/Program Files/nodejs/npm.cmd' install --no-package-lock --ignore-scripts --offline --cache C:/Users/ForestHill/AppData/Local/npm-cache
& 'C:/Program Files/nodejs/node.exe' node_modules/typescript/bin/tsc --noEmit --pretty false
& 'C:/Program Files/nodejs/node.exe' node_modules/next/dist/bin/next build
```

安装已有依赖成功，未修改包清单或锁文件。TypeScript独立检查通过；生产构建通过，11个静态页面生成。第一次类型检查发生在安装未结束时，声明文件不完整；安装完成后再次执行通过。`git diff --check` 通过。

## 浏览器验收与截图

可复跑脚本：`test/account_visibility_browser_server.py`、`test/account_visibility_browser.cjs`。先构建，再从根目录用两个终端执行：

```powershell
# 专用服务器进程；不要继承整套测试的鉴权值。
$env:CHATGPT2API_AUTH_KEY = 'ticket-16-browser'
& C:/Users/ForestHill/Documents/chatgpt2api/.venv/runtime/Scripts/python.exe -m test.account_visibility_browser_server

# 另一个终端
& 'C:/Program Files/nodejs/node.exe' test/account_visibility_browser.cjs
```

服务器仅监听 `127.0.0.1:43260`，同时提供生产静态页面及真实账号API，每次启动使用新的系统临时JSON目录；`/qa/reload` 重建真实服务对象读回同一存储，`/qa/rotate` 触发受控OAuth。不要挂载这些验收入口到生产应用。脚本使用5个 `.test` 假账号、独立无头Chrome context并封锁外网；支持 `PLAYWRIGHT_MODULE`、`CHROME_PATH` 覆盖本机工具路径。需要全新测试服务器以重复执行全流程。

两次实际浏览器运行通过，第二次补充窄屏隐藏找回及计时。包含实际下载文件读取、桌面1600×1000和窄屏390×844截图、独立context重新登录；桌面页面采集的脚本错误为空，窄屏页面未单独收集脚本错误。

验收结束已按PID和完整命令核对并停止两次本票服务的父子进程，浏览器脚本通过 `finally` 关闭Chrome，清理本票临时日志。未停止其他票或用户服务。

- [默认隐藏与监控置顶](16-evidence/default.png)
- [显示隐藏账号及找回入口](16-evidence/show-hidden.png)
- [用途切组、令牌轮换与重载后的顺序](16-evidence/reloaded-order.png)
- [窄屏键盘移动及焦点](16-evidence/narrow-keyboard.png)
- [窄屏滚动至隐藏/找回操作区](16-evidence/narrow-restore.png)
- [实际耗时样本](16-evidence/timings.json)：5账号，本次浏览器从登录导航到列表就绪约1747ms，两次排序HTTP请求约13.5/14.3ms。仅为本机小规模样本，未声称验证大型账号池性能。

## Standards

独立只读子代理审查 `git diff d20ae3360ec5852a7d1160356669e2a8d8f63857...HEAD`（实施提交 `6b9f716`）：**0项行动项**。对照AGENTS、两项代理文档、CONTEXT和两项ADR，未发现文档规范违反或值得修改的基线代码异味。隐藏与使用状态分离符合领域定义；前端排序兼容既有变更响应仍按存储顺序返回列表的路径。

## Spec

独立只读子代理审查同一固定点：**0项行动项**。US100/107的独立隐藏和找回、US105的组内顺序和服务端验证、US108/109的维护/统计/消费/导出均符合本票；用途切组追加到目标组末尾属于合理的未规定位置选择。审查者静态阅读实现和测试，未自行重跑浏览器。

两轴各0项行动项，无最高严重度项。

证据提交 `2c6b2e9` 再审：Standards无新行动项；Spec无实现问题，建议收窄两处报告表述（同页焦点保留、脚本错误仅桌面采集），已在本文明确实际验收范围。

## 集成边界与剩余限制

本票保留既有物理删除和自动移除分支，已将相关上下文告知主对话；票17独立取消这些行为。特别是旧版 `update_account` 的限流自动移除逻辑可能在隐藏更新时触发，本票没有抢改票17范围，最终由主对话联合集成验证。

仍有上述5项既有离线失败。没有本票未解决的审查行动项。生产代码采用现有存储写入语义，未新建存储子系统；前端无拖拽依赖。主对话负责按提交顺序集成，不应覆盖17删除区域或其他票的独立变更。
