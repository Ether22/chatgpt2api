# 10：单张结果真实删除与引用保护

工作树 `C:/Users/ForestHill/.codex/worktrees/03cb/chatgpt2api`，分支 `codex/image-task-10`；固定审查基线 `b471218793bfd3e36eaee8d69970f08fb339c624`；实际模型/强度 `gpt-6-astra / high`。仅本票本地实施，不自行集成或推送。

## 当前实现

- `DELETE /api/image-conversations/{conversation_id}/turns/{turn_id}/images/{task_id}` 校验登录归属及轮次成员，只接受 success 任务。先提交 task 墓碑，FastAPI BackgroundTasks 调用独立 `cleanup_result`，不取消上游、不修改05运行/恢复协议。
- task 保留原 task_ids/status/data 和附加字段，追加 `result_deleted` / `result_cleanup={state,error?,updated_at}`；状态 pending/complete/retained/error。公开已删 task 不返回 data；会话 images 和目标导航排除墓碑，原 task_ids 的一基 ordinal 决定编号/下载名。`resultCleanups` 返回可见清理反馈，不含可用 URL。
- 复用04 `_references` 的 ordinary/imports/turn_ids 和现有 task data 检查持有者，没有另建引用账本。单图删除不释放仍存在的轮次参考快照；输入释放也保护仍有效的生成结果；最后释放经原 `release_turn_reference`/`release_reference`/结果删除清理物理文件。retained 支持再次检查清理。
- storage.delete 不吞 both 模式远端错误；清理原图、两种缩略路径、标签、配置的存储副本。失败保留 SQLite 索引、目标及副本状态，重试只面向剩余副本；新保存图片记录不含凭据的 storage_target，目标改变时明确失败。公共 gallery 不展示 deleting/result_hidden，仍有生成别名的共享文件正常展示。
- 生成结果卡片及共用查看器提供确认删除；参考图不传删除回调。Delete 打开确认、Enter 确认、Esc 取消。确认后即时隐藏并切换相邻图，最后一张关闭查看器。独立清理刷新不依赖 queued/running；pending/error/retained 均有重试入口。请求显式绑定 authKey，供主集成05 identityRequest 时沿用。
- `remove_tags` 不再将读取失败或损坏标签当成空记录，错误进入可重试清理状态。

## 已执行验证（审查前检查点）

- TDD：新增单图 DELETE 初始404；标签读取故障被吞；释放输入误删共享生成结果，均先通过真实公开边界复现后修复。
- `python -m pytest test/test_image_result_deletion.py test/test_image_references_http.py -q --tb=short`：24 通过（本票9项 + 04引用15项）。覆盖local/webdav/both、身份、原序号、原图/缩略/标签、副本失败及目的地变化、共享生成别名、ordinary/imports/轮次最后释放、慢WebDAV不阻塞GET、pending实例重载。
- 完整离线检查脚本 `17-offline-check.py`：审查前一次 242 项及7子场景通过，43.92s；新增最后一项共享引用保护后最终整套待复跑。
- 独立 TypeScript `node node_modules/typescript/bin/tsc --noEmit --pretty false` 通过；Next 生产构建通过，11静态页面；最后清理反馈位置微调后待最终复验。
- Chrome真实生产页：取消、跨身份、确认后39ms隐藏、2秒受控WebDAV pending→error、刷新仍隐藏及重试、390px无横向溢出、参考图无删除、Delete/Enter/Esc、邻图切换、最后图关闭、删除后 `image-3.png` 原序号下载，全部通过。无浏览器pageerror；删除全程消费计数保持3。
- 证据在 `10-evidence/`，脚本 `10-browser-server.py` / `10-browser-check.cjs`；最近一次截图需按反馈位置更新后复拍。测试服务监听43200，仅本票临时存储和受控上游/WebDAV；最终停止PID另记。

## 审查与剩余

Standards/Spec 独立审查待执行；本文件为待审本地检查点，尚不宣称最终完成。11的范围/在途取消和12完整底部操作/本轮下载均未扩做。单张引用保护扫描已有任务/引用集合，代码标明批量性能需测量后优化；未引入新依赖/引用数据库。
