# 10：单张结果真实删除与引用保护

本票已完成本地实施、验收、双轴审查和提交，等待主任务集成。

工作树：`C:/Users/ForestHill/.codex/worktrees/03cb/chatgpt2api`；分支：`codex/image-task-10`；固定基线：`b471218793bfd3e36eaee8d69970f08fb339c624`；实际模型/强度：`gpt-6-astra / high`。

## 逐项验收

- [x] 生成结果卡片和查看器具有单图删除确认；参考图不传删除回调。纯 Prompt 隐藏保留图片、配置与快照。真实浏览器验证取消、Delete 打开确认、Enter 确认、Esc 仅取消确认且保留查看器、邻图切换及最后一张关闭。
- [x] 引用保护使用现有 task data 与04 `_references` 的 ordinary/imports/turn_ids，没有第二份引用账本。结果删除不释放仍存在的轮次快照；输入释放也保护有效生成结果。共享生成别名正常显示，最后结果/输入/轮次引用释放才物理清理。
- [x] 原图、两种缩略路径、标签及 local/WebDAV/both 副本真实清除。错误保留必要索引/副本状态/真实原因；重载仍隐藏且可重试。坏 JSON 或非对象标签不会被当成空记录；结果清理重试不重复已成功删除的 WebDAV 副本。
- [x] 已确认删除在后台启动前中断也不会从图库、原图、缩略入口恢复。task 墓碑及 image_index 可见性通过现有两库的 SQLite ATTACH 事务一起提交；任务提交或索引写失败均不隐藏记录、不删除物理文件。只接受磁盘 delete/truncate/persist journal，明确拒绝 WAL/memory/off 等不满足跨库原子性前提的配置。[SQLite 官方原子性条件](https://www.sqlite.org/lang_attach.html)
- [x] 删除不发送上游取消或新增消费。慢 WebDAV 清理时会话 GET 可完成，pending 在没有活跃生图任务时仍刷新；pending/error/retained 均可显式重试。后台启动前重载的 pending 可完成清理。
- [x] task/task_ids 和原逻辑序号保留。删除中间第2张后其他图片仍为1、3；错误图隐藏也不改变 ordinal。真实下载名仍为 `image-3.png`。

## 接口与共享修改范围

新增 `DELETE /api/image-conversations/{conversation_id}/turns/{turn_id}/images/{task_id}`。服务器核对登录身份、会话、轮次和 task 成员，只接受 success。独立 `delete_result` 提交不可见状态，FastAPI BackgroundTasks 调用独立 `cleanup_result`。不修改05 `_run/_record_failure/start/recover` 或发送/账号协议。

task 保留原 status/data/附加字段，追加 `result_deleted` 与 `result_cleanup={state,error?,updated_at}`；state 为 pending/complete/retained/error，时间明确 +08:00。公开已删 task 不返回 data；公开会话 images/目标定位过滤墓碑，原 task_ids 枚举得到一基 `ordinal`；`resultCleanups` 提供清理反馈，不暴露可用 URL。

`image_rows.save_result_deletion` 只原子写变化 task 与相关 image_index 行，未增加 schema、数据库或旧数据迁移。既有 `save` 抽出同样的行写入循环；05 `_save_locked` 契约完全不变。持有者判断复用现有记录；标签所有读改写共用 RLock，保存使用临时文件、fsync、replace，失败保留原标签。

storage.save/sync_all 记录实际副本目标（URL/root/用户名，不复制密码）；`storage_target` 与04规范化共用同一实现，用户名保持原样。local 保存后新同步到 WebDAV 的副本可正常删除；已有远端副本的定位字段保留，配置目的地改变时清理明确失败，恢复原目的地后重试。`deleting/result_hidden` 阻止已删结果从图库重现；其他有效生成别名正常显示，参考持有仍可读取。

前端仅增加删除 DTO/API、原 ordinal、确认、局部即时隐藏及独立清理刷新。`deleteImageResult(authKey,...)` 显式绑定 Authorization 且禁用跨身份401跳转；主集成05后可直接接同一 `identityRequest`。查看器仅增加可选删除回调及确认取消保护；12完整底部操作/本轮下载布局仍由12完成。

## 实际命令与结果

以下命令均从本票工作树执行；Python 只使用共用只读运行时，未导入主目录实际 data/config。

```powershell
$env:CHATGPT2API_AUTH_KEY = 'chatgpt2api'
& 'C:/Users/ForestHill/Documents/chatgpt2api/.venv/runtime/Scripts/python.exe' -m pytest test/test_image_result_deletion.py -q --tb=short
& 'C:/Users/ForestHill/Documents/chatgpt2api/.venv/runtime/Scripts/python.exe' .scratch/image-workflow-upgrade-delivery/reports/17-offline-check.py
# web 目录
& 'C:/Program Files/nodejs/node.exe' node_modules/typescript/bin/tsc --noEmit --pretty false
& 'C:/Program Files/nodejs/node.exe' node_modules/next/dist/bin/next build
# 根目录；服务通过 Hidden Start-Process 启动
& 'C:/Program Files/nodejs/node.exe' .scratch/image-workflow-upgrade-delivery/reports/10-browser-check.cjs
git diff b471218793bfd3e36eaee8d69970f08fb339c624...HEAD --check
```

- 专项：**19 项通过**。包含跨身份、local/WebDAV/both、目标改变、双向共享引用、原序号、慢远端、后台启动前重载、任务库提交失败、索引写失败、两库 WAL 拒绝、缩略/标签读写失败、并发单删标签竞争、同步新增副本、坏 JSON/非对象标签。
- 完整离线：**253 项及7子场景通过，39.08s**。按 VALIDATION 排除真实消费联调；仅既有 anyio 导入顺序 warning。未跳过旧 settle 或 JSON/file_id 断言。
- 独立 TypeScript 与 Next 生产构建均通过，11静态页面。依赖独立复制到本树，未使用共享可变目录链接、未新增依赖。
- 浏览器：真实 Chrome、生产导出页、真实HTTP/临时SQLite/真实PNG、受控 WebDAV；1440×1000及390×700。确认后实测 **31ms** 隐藏；远端延迟2s时先 pending、再真实 error；刷新后仍隐藏并可重试；没有 queued/running 也自动更新到完成。全部删除操作后上游消费计数始终为3，最终本地及远端仅保留1张仍在用的参考图。无 pageerror。
- [浏览器结果及文件状态](10-evidence/browser.json)、[桌面等待清理](10-evidence/pending-desktop.png)、[窄屏失败与重试](10-evidence/retry-narrow.png)、[最后结果删除](10-evidence/last-result-deleted.png)、[实际原编号下载](10-evidence/image-3.png)。截图已目视复核；首次截图未包含反馈区后调整反馈位置并完成复拍。
- `git diff --check` 通过。最终服务PID **43716/32456** 已逐个核实 `10-browser-server.py` 命令后停止，43200/43201监听数与剩余测试进程数均为0。

## Standards

独立代理初审1项判断性重复代码：storage_target 与04规范化重复且用户名处理不同；已统一。最终固定范围 `b471218...19dacc3`：**0 项硬性违规、0 项需处理的 smell**。代理补核坏标签进度及 Esc 修正，未发现新增行动项。

## Spec

独立代理初审2项P1：后台启动前中断导致图库可复活；local→WebDAV同步后目标记录过期阻止删除。均以真实临时HTTP/SQLite复现，修复并加入回归。最终固定范围 `b471218...19dacc3`：**0 项行动项**，无新增缺口或扩范围。

## 可集成提交与后续边界

按顺序集成全部实现提交：

1. `fd91a6c6c2777d4d6a830738f16c67a6c7fce5ff`：完整单图删除、UI、引用保护及初始证据。
2. `42dc177d4f7f3da95312cd787552994396607f5b`：跨库原子不可见性、同步目标、标签并发与原子保存。
3. `19dacc314b1422337ea10c36d3e8e1cd7d869054`：坏标签保留副本进度、快速 Esc 取消及最终浏览器证据。
4. 本报告/验收状态收尾提交为交接时本分支 HEAD，完整SHA在最终交接消息列出。

无本票未完成行动项。单图引用保护扫描既有任务/引用集合，已用 ponytail 注释记录其规模上限，11做范围/在途与批量性能时可按实测扩展；未抢改相关协议。未读写主实际 data/config，未迁移旧数据，未消费真实额度，未推送、merge/rebase/cherry-pick，未改 PROGRESS 或其他票报告。

## 主集成补充验证

2026-09-08：四提交已主集成，主c7fe210使用05 identityRequest。新组合回归证实删除未推进版本会被05B增量轮询遗漏；9c9d449将墓碑/清理状态与任务版本一起保存，前端收到result_deleted刷新当前有限页。真实HTTP重启pending与完成变更查询先红后绿；两独立登录浏览器无重载同步验证通过。完整组合282项与7子场景通过、tsc/11页构建通过，43028浏览器51ms即时隐藏，其余原10场景全过且0脚本错误。原验收脚本已追加第二浏览器步骤；主验证证据位于隔离工作树data/10-evidence。
