# 18：北京时间（阶段 A 可集成；整票未完成，等待阶段 B）

- 任务 ID：`01a07cff-709a-7512-ac93-8f632637e9e3`。
- 工作树：`C:/Users/ForestHill/.codex/worktrees/a99c/chatgpt2api`。
- 分支：`codex/image-task-18`；派发与审查固定点：`30785289d9494c7d0b04ebfecda411fc138a9ea7`。
- 阶段 A 范围：`services/auth_service.py`、`services/log_service.py`、`services/backup_service.py`、`services/editable_file_task_service.py`；用户密钥/备份设置卡片和日志页；最小时间工具与验收。
- 当前可调用工具中没有 `send_message_to_thread`，启动信息通过本报告及任务 commentary 提供主任务读取。未自行合并、变基或修改其他票、PROGRESS。

## 共享接口（供主任务通知后续票）

`utils.business_time`：`BEIJING` 为标准库固定 UTC+08:00；`beijing_now()` 返回有时区 datetime；`beijing_iso(value=None)` 接受当前时间、带时区 datetime、epoch 秒、ISO 字符串，输出保留瞬时的 `+08:00` ISO。

有时区 UTC 输入转换到北京时间；旧无时区/无效字符串原样返回，不猜测或迁移历史。无时区 datetime 拒绝，避免隐式服务器时区。业务文件日期可用 `beijing_now().strftime(...)`，文件名采用 `%Y%m%dT%H%M%S%z`，如 `20260908T000001+0800`。JWT、AWS V4、外部协议及 epoch 耗时仍由原路径负责。

前端 `web/src/lib/business-time.ts` 导出 `formatBeijingDateTime(value?: string | null)`，使用显式 `Asia/Shanghai` 和 24 小时制，格式为 `2026/09/08 00:00:01 北京时间`；接受带 offset / Z 的业务日期，缺值显示 `—`，不推断历史无时区值。

## 阶段 A 逐项验收

| 链路 | 实施及实际证据 |
| --- | --- |
| 用户密钥 | 新建及最近使用保存 `+08:00`；公开读取转换已知 UTC 输入且不改写历史创建值；真实临时 JSON 存储验证 59 秒不刷新、60 秒刷新。鉴权 UTC 时钟与间隔未变。 |
| 日志 | 新记录和调用起止日期写 `+08:00`；公开读取先将明确 UTC 转为北京时间再按业务日筛选。午夜前后 2 秒仍为 `2000 ms`；读取不改写文件，删除其他记录也不迁移日期。日志页和详情使用共用格式工具。 |
| 备份 | 对象文件名包含北京时间和 `+0800`，元数据、任务状态、对象列表和详情转换为 `+08:00`。真实压缩包通过受控 R2 transport 上传和读回。UTC 输入 `2026-09-07T16:00:01Z` 对应 `2026-09-08T00:00:01+08:00`；签名仍用 `20260907T160001Z` 和 `20260907/auto/s3/aws4_request`。调度间隔不重复触发，tar mtime 仍为 epoch。 |
| 可编辑文件任务 | 通过公开 PSD 提交、轮询和真实任务文件验证业务日期；缺少参考图在访问账号/上游前失败，失败日志日期一致，elapsed 秒保持原义。 |
| 浏览器 | 真实 Chrome、三个独立 context：UTC / America/Los_Angeles / Asia/Shanghai。实际登录、本地 API、临时存储、设置页切换、备份详情、日志详情。另在受控 HTTP 边界注入原始 UTC 行，验证前端自身的转换。三个 context 均显示午夜后 `2026/09/08 00:00:01 北京时间`，午夜前显示 `2026/09/07 23:59:59 北京时间`。外部浏览器请求全部拦截。 |
| 服务器默认时区 | 独立 Python 子进程实际设置系统 C runtime 本地时区 UTC0 / PST8 / CST-8，先确认本地时间确实分别为 16:00:01 / 08:00:01 / 次日00:00:01，再验证同一业务输出始终为北京时间。未修改系统全局时区。 |

`utils/log.py` 本身不输出日期，不需增加格式器；JWT、外部协议、AWS V4 UTC 和耗时没有机械加 8 小时。没有新依赖、旧数据迁移或无关票修复。

## 命令与结果

均从本工作树运行，Python 为 `C:/Users/ForestHill/Documents/chatgpt2api/.venv/runtime/Scripts/python.exe`，Node 为 `C:/Program Files/nodejs/node.exe`，测试键仅 `CHATGPT2API_AUTH_KEY=chatgpt2api`。

- `python -m pytest test/test_beijing_business_time.py -q`：最终 6 通过。密钥、日志、备份、可编辑任务各自先观察业务断言失败，再实施到通过；服务器 TZ 检查修正了测试日志输出和 Windows CRT 的三字母时区写法。
- `python -m pytest test/test_beijing_business_time.py test/test_account_image_capabilities.py -q`：当时前四项完成后 17 通过。
- 完整离线套件按 `VALIDATION.md` 排除手工真实上游脚本，并显式选择 `test_v1_models.py::ModelListTests` 的两项 mock 测试。用 `unittest.mock.patch('curl_cffi.requests.get', offline_get)` 仅为 `https://example.com/a.png` 返回受控图片字节，其他未预期 URL 抛错：**149 通过、5 既有失败，42.20 秒**。失败为 1 项 settle 及 4 项参考图旧断言，归属 05/04；没有掩盖或放宽断言。首次命令写错旧模型测试类名导致未执行，修正后才记录上述结果。
- `node node_modules/typescript/bin/tsc --noEmit --pretty false`（web cwd）：退出 0。
- `node node_modules/next/dist/bin/next build`（web cwd）：退出 0，11 静态页面；独立 tsc 同样通过，未依赖 build 的跳过类型检查选项。
- `python -m test.beijing_business_time_server`：隐藏窗口、本票端口 `43281`，lifespan 关闭，未启动真实账号维护/备份调度；使用实际 HTTP 路由和临时 JSON/R2 数据。首次以主任务既有构建只读复现前端红灯：UTC 浏览器错误显示 `2026/09/07 16:00`；随后切换本工作树生产构建。
- `node test/beijing_business_time_browser.cjs`：退出 0，三个时区全部通过。随后增加真实下载事件的 suggestedFilename 断言并重新执行：三个时区下载均保留 `backup-20260908T000001+0800-*.tar.gz`，退出 0。默认浏览器模块和 Chrome 路径见脚本，可通过环境变量覆盖。
- 安装现有依赖时镜像连接重置；官方 registry 补齐也较慢，停止后用 `robocopy .../.venv/integration-check/web/node_modules web/node_modules /E /IS /IT /NFL /NDL /NJH /NJS /NP /R:1 /W:1` 复制主任务已验证依赖供本工作树独立使用（exit 1 为成功复制）。没有修改依赖清单或提交缓存。

## 浏览器证据

本工作树 `test/.beijing-evidence/results.json` 保存三个时区的实测结果；每个时区均有 keys / backup / logs PNG。已实际打开复核 `America-Los_Angeles-keys.png`、`UTC-backup.png`、`Asia-Shanghai-logs.png`，时间清晰、无遮挡。脚本可重新生成全部证据，生成物不提交。

## 双轴审查

固定实际提交 `815436d`，差异 `git diff 30785289d9494c7d0b04ebfecda411fc138a9ea7...HEAD`。Standards 与阶段 A Spec 两个独立代理并行完成，均为 gpt-6-astra/high；没有产品代码修复项。

### Standards

0 项明文规范违规，0 项需处理的 baseline smell。已核对全部 13 个文件，以及 AGENTS.md、issue-tracker.md、domain.md、CONTEXT.md 和两项 ADR。时间规则集中于共用工具，前端重复格式器已删除；未发现值得增加抽象来解决的重复或额外复杂度。

### Spec

阶段 A：遗漏/部分实现 0 项、范围扩张 0 项、实现错误 0 项。符合 US112–114 的业务日期统一、环境时区独立、瞬时计算与协议 UTC 保持原义。独立审查再次运行目标测试：6 passed；另核对浏览器脚本，但审查代理未重复运行浏览器。阶段 B 获准延期，不计为 A 缺陷。

汇总：Standards 0，Spec 0；两个轴均无待修复项。

## 交付与进程清理

- 阶段 A 产品代码及基础验收提交：`815436d`；其后仅补充浏览器下载断言和本报告结论。主任务按本分支提交顺序集成。
- 临时服务器 9108（基线前端复现）、29892（本工作树构建验收）均在核对 PID 和 `python -m test.beijing_business_time_server` 命令后停止。43281 无监听。浏览器均在 finally 中关闭；未停止其他票进程。
- 业务代码无新依赖、配置或账户数据提交；本票已完成验收的生成物仅留本工作树供复核。

## 剩余阶段 B

等待主任务先集成验证 A，然后由主任务将同一工作树同步到最新集成基线并明确续办。复核图片/任务/图库/查看器、账号 API/服务/导出、CPA/Sub2API 作业及其他票新增业务日期。当前不会改这些活动区域，不把部分交付标为票 18 完成。
