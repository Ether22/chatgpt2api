# 02 OAuth 弹窗可达性实施报告

- 工作树：`C:/Users/ForestHill/.codex/worktrees/e6ce/chatgpt2api`
- 分支：`codex/image-task-02`
- 派发与审查固定点：`2e3cba11c3d6e85a0daba22b4d4292cecd95af4d`
- 实施提交：`67ab6b6`；本报告及浏览器证据由后续提交补充。未自行集成。

## 修改范围

仅应用文件 `web/src/app/accounts/components/account-import-dialog.tsx`：弹窗按 `100dvh - 2rem` 限高，标题说明与正文同处滚动容器，操作区不收缩；长授权 URL 限高、可滚动及键盘聚焦，仍保留全文。邮箱和 callback 标签关联输入框。

浏览器实测发现 Radix 焦点循环返回首控件时会保留原滚动位置，故在本地正文容器聚焦时调用原生 `scrollIntoView({block: "nearest"})`。授权启动、复制、回调提交逻辑未改；共享 Dialog、账号页面/API、PROGRESS 均未修改。

## 验收与浏览器证据

- US115：低高度下弹窗及底部操作区位于视口内，完成导入与取消可点击。
- US116：合成授权 URL 为 2,380 字符；复制结果与完整原字符串严格相等。正文与 URL 都可滚动。
- US117：每个尺寸用独立空白 Chrome context 加载页面；Tab 循环检查聚焦元素处于视口内，遍历到复制、callback、完成导入和取消。callback 提交收到原 session_id 和输入值，成功后弹窗关闭；重新打开后取消关闭。

| CSS 视口 | 弹窗 x/y/宽/高 | 复制、键盘、提交、取消 |
| --- | --- | --- |
| 1280×600 | 360 / 16 / 560 / 568 | 通过 |
| 375×667 | 15 / 16 / 345 / 635 | 通过 |
| 667×375 | 53.5 / 16 / 560 / 343 | 通过 |
| 640×360 | 40 / 16 / 560 / 328 | 通过 |
| 320×480 | 12.8 / 16 / 294.4 / 448 | 通过 |

640×360 是 1280×720 在 200% 桌面缩放时的等效 CSS 视口。没有声称操作过浏览器原生缩放菜单或手机软键盘。动态 `setViewportSize` 在本机无头 Chrome 出现 innerWidth 改变但 vw/dvh 与媒体查询仍沿用旧值，故各尺寸均独立加载，未把该仿真异常当作应用修复。

修改前脚本在 1280×600 明确失败于 `dialog must fit viewport`，保存了[原始越界截图](02-evidence/oauth-before.png)。修复后已目视检查[窄屏截图](02-evidence/oauth-375x667.png)与[低高度截图](02-evidence/oauth-640x360.png)，几何与行为结果见 [results.json](02-evidence/results.json)。

测试只运行本票 43120 前端；API 由 Playwright 在浏览器网络边界返回合成响应，其他外部地址全部中止。没有运行真实 OAuth、读取真实账号或消费上游额度。此项验证的是前端行为，不宣称真实上游换 token 已验证。

## 实际命令与结果

均从本工作树运行（前端安装命令在 `web` 中）：

```powershell
npm.cmd install --no-package-lock --no-audit --no-fund --fetch-retries=0
node web/node_modules/typescript/bin/tsc --noEmit --project web/tsconfig.json
git diff --check
node data/verify-oauth.cjs --baseline
node data/verify-oauth.cjs 1280 600
node data/verify-oauth.cjs 375 667
node data/verify-oauth.cjs 667 375
node data/verify-oauth.cjs 640 360
node data/verify-oauth.cjs 320 480
```

类型检查多次通过，最终通过；diff 空白检查通过。基线命令在修改前执行且预期失败，五组最终浏览器命令全部退出 0。[浏览器脚本](02-evidence/verify-oauth.cjs)保留实际运行版本，需已启动 43120 的本树 Next 开发服务；脚本依赖本机 bundled Playwright 与 Chrome 绝对路径。截图/输出路径在工作树忽略的 `data/`。首次开发服务冷编译出现过超时，预热后完成验证。

Python 使用主对话提供的共享 Python 3.13.15 解释器，但 cwd、配置与数据均在本工作树：

```powershell
$env:CHATGPT2API_AUTH_KEY='ticket-02-only'
& C:/Users/ForestHill/Documents/chatgpt2api/.venv/runtime/Scripts/python.exe -m pytest test/test_account_export.py test/test_account_image_capabilities.py -q
# 16 passed

$env:CHATGPT2API_AUTH_KEY='chatgpt2api'
$env:PYTHONIOENCODING='utf-8'
$task02Tests = @(rg -l 'import unittest|import pytest' test) | Where-Object { $_ -notmatch 'test_v1_(chat_completions|images_generations|images_edits\.py|messages|responses)\.?' }
& C:/Users/ForestHill/Documents/chatgpt2api/.venv/runtime/Scripts/python.exe -m pytest @task02Tests --deselect=test/test_v1_models.py::ModelListTests::test_list_models_http -q
# 134 passed, 5 failed, 1 deselected, 1 warning
```

完整离线集合包含现有 unittest 与数据库 pytest。初次使用不同测试键导致 14 项 401，改用旧测试硬编码的 `chatgpt2api` 后排除。测试键只用于本树测试。5 项失败已由主对话在原始基线独立复现，移交票04/05，不在本票扩改后端：

1. `test_multi_image_results.py::MultiImageResultTests::test_poll_waits_for_generated_asset_ids_to_settle`：实际只返回 file-one，旧断言期望 file-two 也已稳定。
2. `test_v1_images_edits_json.py::ImageEditsJsonApiTests::test_image_edit_accepts_json_image_url`：默认文件名 image_url.png 与旧断言 image_1.png 不同。
3. 同类 `test_image_edit_accepts_json_multiple_images_and_b64_json`：默认文件名旧断言不符。
4. 同类 `test_image_edit_rejects_json_without_image`：缺图错误文案旧断言不符。
5. 同类 `test_image_edit_rejects_remote_json_url`：实现已支持远程 URL，旧断言期望拒绝；本次受限网络连接失败，未成功访问远端，之后未在联网模式重试。

现有 SQLAlchemy `declarative_base()` 弃用警告 1 条。依赖安装未修改需求文件或锁文件；初次尝试 pip editable 被项目多顶层包布局拒绝，不影响后续共享解释器测试。

明确未运行的手动/凭据联调：`test_v1_chat_completions.py`、`test_v1_images_generations.py`、`test_v1_images_edits.py`、`test_v1_messages.py`、`test_v1_responses.py`，以及 `test_v1_models.py::test_list_models_http`；另有直接执行脚本 `test_image.py`、`test_generations.py`、`test_generations_url.py`、`test_codex_4k.py`、`test_image_output_tokens.py`、`test_gpt_search.py`、`test_gpt_psd.py`、`test_gpt_ppt.py` 和 `scripts/test_storage.py`、`scripts/test_outlook_token_mailbox.py`。这些不冒充离线测试通过。

## Standards

独立子代理审查 `git diff 2e3cba11c3d6e85a0daba22b4d4292cecd95af4d...HEAD`（HEAD 为 67ab6b6）：0 项发现。局部布局、焦点可见性和标签关联符合限定范围；与 AGENTS.md、领域术语和 ADR 无冲突，未发现新增重复逻辑或无需求抽象。

## Spec

独立子代理审查同一固定差异：0 项发现。US115–117 的视口限制、滚动、操作区、完整 URL 与焦点可达均有实现，无授权流程修改或共享弹窗范围扩大。审查时其余尺寸仍待验证，现五组结果已补齐。

两轴均 0 项行动项。剩余限制为上述 5 项基线失败及真实 OAuth/原生缩放菜单未联调。
