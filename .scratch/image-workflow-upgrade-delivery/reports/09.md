# 09：重跑与复用持续归入原条目

## 交付状态

- 实际任务：01a07dc4-78aa-7f32-9ec3-ef5f49828baa；模型/强度：gpt-6-astra / high。
- 工作树：`C:/Users/ForestHill/.codex/worktrees/7d14/chatgpt2api`；分支：`codex/image-task-09`。
- 干净派发与审查固定点：`f6d26badc4a532c84e29501fac1ca53d3a6cc034`。
- 实施提交：`44df16b887a3436a85bde6bb2893eb436150eb82`；验收脚本、证据与本票勾选提交：`2dfe6176e0e85068ddcdfecd3da66d71687a2484`；本报告及未用测试端点精简另作为最终报告提交，在最终回报给出SHA。
- 本票完成，交主任务逐个集成。未合并、变基、cherry-pick或推送；未修改PROGRESS或其他票报告。

## 逐项验收

| 要求 | 实现与实证 |
| --- | --- |
| 重跑、复用和再次复用新增原条目轮次，不覆盖旧图 | 服务端从同会话原轮查来源；HTTP三次复用仅一个sourceEntryId，原轮完整相等；真实浏览器1→100→1→1→1共104张，旧结果和MD元数据保留。 |
| 原快照不受当前导入区改写或清空影响 | HTTP改未来Prompt/output_name后，重跑仍为原Prompt、1200x800和main.jpg；浏览器清空MD后重跑、复用、下载仍保留original-document.md及original主体。 |
| 允许改变Prompt、尺寸、参考图 | 浏览器复用修改Prompt、900x700；参考图从[one,two]改为[two,replacement]，新轮同来源，旧轮仍能读出原图原字节。 |
| 纯Prompt隐藏保留配置与复用入口 | 公开历史保留原Prompt；隐藏后按钮仍可回填，真实浏览器再次提交成功且原来源不变。 |
| 每轮1–100数量 | 新轮数量为文本数字输入；空值、0、101、1.5、1e2、-1、前导空格禁提交；HTTP另覆盖bool/数字字符串。浏览器真实100张成功、逐轮1成功；390×600、320×360指针点击、输入及重新生成控件可达。 |
| 会话边界清楚、同名新产品独立 | 同会话复用保存来源选择；切换会话清空来源但保留编辑值，按目标会话普通新条目提交；新建清空草稿与来源；成功提交清空本次来源。提供“改为新条目”显式解除来源。两个操作均有真实浏览器验证。已有08同文档标识重新导入/新会话独立测试全部回归通过。 |
| 来源安全 | 新增source_turn_id仅能引用明确指定且属于当前身份的会话轮次；跨身份/会话404，省略会话或不匹配source_entry_id为400。客户端不能直接提交md元数据（DTO extra=forbid）。 |
| pending参考图真实等待 | 重跑绑定原reference ID和既有turn_ids，未ready不伪装ready；HTTP原上传完成后两轮成功。取消后上传新同名文件，两轮均失败且0消费，之后重跑明确拒绝。 |
| retain失败不污染输入 | Promise.allSettled后才替换整套编辑配置。任一失败释放所有新尝试的hold（包括丢响应但服务器成功的retain）；旧输入不释放。浏览器实际让第二retain服务器成功后丢响应，最终只保留旧draft。延迟retain期间新建会话，迟到成功只释放hold、不覆盖新draft。 |
| 丢响应重试仅一次消费 | 浏览器首POST经真实服务器接受后abort响应，回到原轮分页点击重跑，第二POST request_id与首个相同；轮数和合成消费数未增加。 |

## 接口及修改范围

- `POST /api/image-conversations/turns`新增可选`source_turn_id`及`rerun`（默认false）。`rerun=true`要求原轮；count仍由本次1–100请求决定，Prompt/model/size/quality/ratio/tier/referenceImages全部取服务器原轮。
- `rerun=false`且指定原轮表示编辑配置复用：采用提交的新配置/有序引用，但来源与MD文档名称、输出名称等取可信原轮；MD参考名和上传ID按本轮实际引用同步。
- 继续使用既有`source_entry_id`合法性检查、请求ID重放、引用账本和任务保存/调度；未新增本地整会话持久化、依赖、数据迁移或独立账本。
- 修改现有提交服务/DTO、store提交映射、page复用/重跑/submit及必要来源草稿、新`RegenerateTurn`数量控件与隐藏后复用入口。未改11删除/迟到状态函数、12 lightbox/下载函数。
- 根已通知并独占`_submit_turns`异常回滚处共享引用对象原地恢复修复；09保持该区域原样，集成时必须保留根的修复。

## 实际命令与结果

所有命令从本工作树运行，Python只读复用`C:/Users/ForestHill/Documents/chatgpt2api/.venv/runtime/Scripts/python.exe`，Node为`C:/Program Files/nodejs/node.exe`。

1. `$env:CHATGPT2API_AUTH_KEY='chatgpt2api'; & '<Python>' -m pytest test/test_image_lineage_http.py -q --tb=short`：11 passed。首个MD快照场景在实现前因新DTO字段extra_forbidden而失败，实现后通过。一次测试断言误把有限页返回当全轮次，已改按公共分页详情读取，未放宽业务断言。
2. `& '<Node>' node_modules/typescript/bin/tsc --noEmit --pretty false`（web目录）：退出0。
3. `& '<Node>' node_modules/next/dist/bin/next build`（web目录）：生产构建成功，11静态页面。没有用build代替独立tsc。
4. `& '<Python>' .scratch/image-workflow-upgrade-delivery/reports/17-offline-check.py`：316 passed、7 subtests passed，76.44秒。仅现有pytest anyio重复导入警告；详情[09-offline.log](09-offline.log)。按VALIDATION排除真实联调脚本，模拟外部边界。
5. 复制主隔离验证树node_modules至本工作树独立目录；无共享可变链接、无新增依赖。
6. `Start-Process ... -WindowStyle Hidden ... 09-browser-server.py`：43190端口，真实临时Auth/任务/SQLite/图片存储与生产web/out，外部生成器只返回合成PNG；没有真实额度消费。
7. `& '<Node>' .scratch/image-workflow-upgrade-delivery/reports/09-browser-check.cjs`：PASS，0页面错误；[主流程结果](09-evidence/browser.json)。100图首次完成5217ms，精简测试服务器后的最终复跑4183ms，仅本地合成样本，不代表真实上游吞吐。主流程包括跨浏览器恢复、下载原PNG与输出名保留。
8. `& '<Node>' .scratch/image-workflow-upgrade-delivery/reports/09-browser-references.cjs`：PASS，0页面错误；[引用竞态结果](09-evidence/reference-races.json)。普通引用替换/旧原字节、retain部分丢响应、换会话迟到retain。
9. `& '<Node>' .scratch/image-workflow-upgrade-delivery/reports/09-browser-switch.cjs`：PASS；[会话边界结果](09-evidence/switch.json)，390/320px实际指针点击输入通过。截图已目视复核：[390×600](09-evidence/controls-390x600.png)、[320×360](09-evidence/controls-320x360.png)、[参考图复用](09-evidence/references-reused.png)、[新会话](09-evidence/new-independent.png)。
10. 初版浏览器脚本存在静态选择器错误（高画质设置按钮、下载按钮、有限页已跳最新）、一份无效PNG夹具；均修正脚本/夹具后重新实际运行，未通过改应用或跳过断言消除。最终脚本可按7→8→9顺序从新启动的隔离服务器复跑；8使用7下载的真实PNG。
11. 测试服务PID29496/34932均用完整命令行核对为09-browser-server.py后停止；之后精简测试服务器再跑7→8→9均PASS并再次核对/停止47876/47068，同脚本Python进程查询为空。早先服务PID15304/7524、45328/38496、44016/40396也逐次核对停止；浏览器在脚本finally关闭。最终模拟abort期间服务器可记录WinError10054连接重置，这是脚本主动丢响应，0页面错误和幂等断言均通过。

## Standards

独立子代理`standards_review`审查`git diff f6d26badc4a532c84e29501fac1ca53d3a6cc034...44df16b`全部6个文件，核对AGENTS、领域文档、两项ADR及code-review smell baseline：0行动项。来源校验集中在既有服务，引用生命周期沿用既有机制，无需额外抽象。

最终复核`f6d26bad...2dfe617`验收文件，发现1项P3判断项：09测试服务器复制了未调用的慢上传/重启控制端点。已删除端点、Event/PngInfo、slow.png和save包装，重新实际执行三组浏览器均PASS；独立Standards复审确认关闭，最终0剩余行动项。

## Spec

独立子代理`spec_review`核对09票及父规格US35/53–57/87：0行动项。验证静态实现涵盖同身份同会话、原快照及MD、flat来源、隐藏后复用、数量范围、retain失败与请求ID重试；动态实证见上述HTTP与浏览器。

最终复核`f6d26bad...2dfe617`新增验收文件和本报告：0行动项；所有验收结论有对应断言或结果，无新增需求偏差。

应用代码双轴初审均0项，无需应用代码修复；最终验收审查的1项测试代码P3已关闭。没有剩余本票功能缺口。真实上游、真实远端存储与主目录实际data/config未使用；跨票组合由主任务在保留其共享回滚修复后复验。主任务已回报组合336 passed及7子场景、tsc/build通过，主浏览器6325ms及references PASS，最终整票状态仍由主任务维护。

## 主对话集成复验（2026-09-08 北京时间）

应用与证据已顺序合入1b667e6、5e5a23b，最终ce8b2ca报告/夹具精简随后补入；应用始终44df16b不变。根逐处保留11删除/迟到、83ea77c引用记录原地更新和664065c提交回滚clear/update。主隔离完整离线336 passed、7 subtests passed（98.72秒），独立tsc及11页生产构建通过。

根43031对生产页面按check→references→switch顺序复跑三组脚本，全部PASS且无页面异常。100张合成重跑6325ms，原MD/图片保留、同ID重试仅一次消费、部分retain丢响应回滚、迟到回复不覆盖新草稿、跨会话来源清除及第二浏览器恢复、实际原PNG下载均通过。根证据位于隔离树data/09-evidence，日志data/after09-*；专用31216/27604已按完整命令核验停止。

人工查看390/320px证据；数量实际指针可达。320×360主页面的结果区域仍受既有头部/分页与composer压缩，已明确交14的主区/导航布局一并改善完整阅读空间；此为整体低高度布局后续项，不改变09已验证的来源行为。最终夹具只删未调用端点，源任务精简后三组浏览器再次通过，根未对不变应用重复验收。
