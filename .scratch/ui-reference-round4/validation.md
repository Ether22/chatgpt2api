# 刷新累计与参考版生图样式验收

2026-09-10，按照用户对 Q1–Q3 的确认完成。

| 范围 | 完成行为 |
|---|---|
| 刷新额度 | 全部、选中和单个账号的额度刷新开始时，卡片显示 0；按本次成功查询的可消费额度累加。失败账号的旧额度不计入，实际账号额度不会因开始刷新而清空。 |
| 恢复额度 | 自动恢复阶段接着刷新阶段累计，不再次归零；独立恢复从 0 开始。密码恢复后的额度查询失败不计入累计。互斥操作在处理中禁用。 |
| 完成与失败 | 结束后同步并显示整个号池总额。若进度查询与账号重载同时失败，仍保留最近一次有效的全池统计。短“刷新”只重新读取账号。 |
| Prompt | 采用参考版右侧摆放、最大宽度、浅色圆角折叠块；默认首行摘要，原生点击或键盘展开/收起。轮次信息在上方，复用和删除在下方；展开正文左对齐。删除之前的整行灰色外框和独立 Prompt 标题栏。 |
| 图片 | 保留 4 张 2×2、6 张 3×2 和手机最多两列；图像区域仍是桌面 224px、手机 160px。成功图片移除固定灰底，圆角直接作用于图像，完整显示原始比例，操作保留在下方。 |

参考 ZIP 仅用于读取布局和样式，没有执行其中代码。当前会话、鉴权、定位、删除、下载及失败操作沿用现有实现。

## 验证

- 28 项后端检查通过，覆盖成功/失败累计、正常使用与监控/禁用的额度分离、恢复成功后的累计，以及原有账号保留和排序逻辑。
- 新增检查验证：本批只累计成功账号的 20；失败账号保存的 99 不加入，未选中账号的 70 不加入；结束全池统计仍为 189，账号实际额度未被清零。
- TypeScript 独立检查通过，生产构建通过，生成 11 个静态页面。
- [浏览器验收](../../test/image_ui_density_browser.cjs)通过：刷新起始 0；部分刷新过程显示 30、完成后显示全池 100；自动恢复接续到 108；独立恢复过程为 0 → 6、结束为全池 106；查询和重载同时失败后保留有效全池统计；千位分隔显示正常。
- 同一浏览器检查通过 1440px/390px 的 4/6 张排布、图片无灰底、图像本身圆角与比例、原生 Prompt 点击展开及 Enter 收起。慢图切换、迟到请求、加载重试、删除、权限失效和身份变更检查继续通过。
- 已查看桌面与手机截图。14 个修改的应用、测试及领域文件与受检副本逐字节一致；`git diff --check` 通过。

所有运行检查均使用 `.venv/integration-check` 隔离环境与合成数据，未使用真实账号。测试服务已关闭，端口 43280 无监听。本轮未提交 Git，也未部署到实际服务。

## 截图

- [Prompt 与四张图片（桌面）](evidence/results-1440.png)
- [六张图片（桌面）](evidence/grid-6-1440.png)
- [Prompt 与图片（手机）](evidence/results-390.png)
- [刷新中显示本次累计](evidence/quota-live.png)

## 2026-09-11：进一步缩小图片

- 按用户后续要求收窄生成图网格：单列最多 224px、双列 460px、三列 696px，不再横向铺满整个结果区域。仍保持 4 张 2×2、6 张 3×2。
- 图片区域由桌面 224px / 手机 160px 调整为桌面 160px / 手机 128px；加载和失败区域同步缩小。保留完整比例、图像圆角、下方信息和操作。参考图标题及缩略图组仍右对齐。
- TypeScript 检查、生产构建和现有浏览器验收通过。新增尺寸和溢出断言确认：1440px / 390px 下图片卡片宽度不超过 224px，实际高度为 160px / 128px，信息与按钮不溢出；原有 Prompt 折叠及大图切换、重试、删除、鉴权检查通过。
- 已查看 [桌面四张](evidence-compact/results-1440.png)、[桌面六张](evidence-compact/grid-6-1440.png) 与 [手机效果](evidence-compact/results-390.png)。使用隔离副本与合成数据；修改的组件和浏览器检查与受检副本逐字节一致。

## 2026-09-11：按参考项目恢复自然尺寸

- 用户反馈上一版太小后，移除了固定卡片宽度与 160px / 128px 高度。桌面采用上传项目的两列、宽屏三列和 16px 列间距，按图像原始比例显示自然高度；手机保留两列和完整图片。此前 4 张 2×2 的固定排布由参考版桌面分列规则替代。
- 每张图片保留已知比例，即使离开可视区域并卸载图片，占位也保持原高度；加载中及失败区域按请求尺寸显示，尺寸自动时使用正方形占位。
- TypeScript 检查及生产构建通过。浏览器覆盖 1440px、1024px、390px：桌面卡片宽度超过上一版 224px，列数随断点为三列 / 两列，图片比例和离屏占位一致，信息与操作不溢出。Prompt 折叠、大图切换、加载重试、删除、鉴权检查继续通过。
- 当前效果：[宽屏四张](evidence-reference-size/results-1440.png)、[宽屏六张](evidence-reference-size/grid-6-1440.png)、[桌面两列](evidence-reference-size/grid-4-1024.png)、[手机](evidence-reference-size/results-390.png)。测试采用隔离副本与合成数据，未部署。

## 2026-09-11：参考单张大小与按行排序（当前版本）

- 用户确认保持参考项目单张大小。四张图固定每行两张，从左到右、从上到下依次为结果 1、2 / 3、4；宽屏下两列组右侧留白，单张不因减少列数而放大。六张图在宽屏下为 1、2、3 / 4、5、6。
- 用按行排列的 CSS Grid 替换按列瀑布流。图片继续按原比例与自然高度显示；同排以最高卡片决定下一排起点，避免视觉顺序被不同图高打乱。后端固定序号与完成先后无关，无需重排或重新编号。
- TypeScript 检查、生产构建和浏览器验收通过。1440px、1024px、390px 下核对真实坐标及独立的结果编号文字，确认行序；同时验证四张与六张布局的单张宽度相同、离屏占位比例不变、信息和操作不溢出。原有 Prompt 与大图操作检查继续通过。
- 已查看 [完整四张排序效果](evidence-order/order-four-desktop.png)、[宽屏六张](evidence-order/grid-6-1440.png)、[手机](evidence-order/results-390.png)。截图使用合成测试图，顺序以各图下方“结果 N”标识为准。
- 源码与受检副本逐字节一致，`git diff --check` 通过。更新了规格及 `CONTEXT.md` 中的“结果序号”定义；使用隔离环境，未部署。
